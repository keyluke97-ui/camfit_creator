# 지명형 협찬 오픈 전 점검

**작성** 2026-08-25 · **대상** 크리에이터 포털 `influencer-portal` + 캠지기측 `camjigi_claude`
**결론** 코드는 끝났습니다. **오픈은 아직 안 됩니다.**

> **2026-08-25 사장님 결정 반영됨.** 각 항목에 `✅ 결정` 으로 표시. 남은 개발은 **§1 제안 수신함** 하나입니다.

---

## 0. 지금 상태 한 눈에

| | |
|---|---|
| 코드 (Task 0~16) | ✅ 전부 완료 |
| 브랜치 `feat/jimyeong-1a-v2` | 🔴 **미푸시** (origin/main 대비 57개 파일) |
| 프로덕션의 포트폴리오 코드 | 🔴 **0줄** — 배포된 적 없음 |
| 프로필 채운 실제 크리에이터 | 🔴 **0명** (354명 중 값 있는 6명 전원 `[TEST]`) |
| 심사 담당·SLA | 🔴 정해진 적 없음 (화면은 이미 "1영업일" 약속 중) |
| Airtable 심사 뷰 | 🔴 **없음** (뷰 9개 전수 확인, 심사 관련 0개) |

---

## 1. 🔴 가장 큰 것 — 크리에이터가 제안을 **거절할 방법이 없습니다**

### 무슨 일이 벌어지나

포털 공개 화면이 크리에이터에게 이렇게 약속합니다.

> 1. 캠지기가 **먼저 입금한 뒤** 제안합니다
> 2. 캠핏이 입금을 확인하고 제안서를 이메일로 보내드려요
> 3. **24~48시간 안에 확인해주세요**
> 4. **응답이 없으면 수락한 것으로 자동 확정됩니다**
> 5. **거절하실 땐 사유를 적어주셔야** 하고, 거절이 반복되면 서비스 이용이 제한될 수 있어요

그런데 **확인·수락·거절할 화면이 어느 쪽에도 없습니다.**

실측:
- 포털 `app/dashboard/` = `page.tsx`, `portfolio` 둘뿐. 제안 관련 라우트 없음
- 포털 `app/api/` 에 제안 관련 엔드포인트 없음
- 캠지기 레포 전수 검색 → `예외거부`·`크리에이터확인중`이 **문서(.md)에만** 있고 코드에 없음

### 그래서 실제로 이렇게 됩니다

```
캠지기가 선입금  →  제안서 이메일 발송  →  크리에이터: 확인·수락·거절할 곳이 없음
```

**캠지기 돈이 이미 들어온 상태에서 제안이 갈 곳을 잃습니다.** 약속한 3·4·5번이 그대로 깨집니다.

> ⚠️ 처음엔 "무응답이니 전원 자동 확정되겠구나" 생각했는데, **자동화까지 확인해보니 자동 확정도 없습니다**(§5-①). 확정도 안 되고 거절도 안 되고 그냥 멈춥니다.

### 이미 스펙은 있습니다

`camjigi_claude/docs/[spec] 크리에이터포털_지명형_제안수신함_빌드스펙_20260715.md`
— **대상 repo가 우리 포털**로 명시된 자기완결 지시서입니다. 2026-07-15에 나왔고 **구현이 안 됐습니다.**

### 결정이 필요합니다

### ✅ 결정 — A안. 제안 수신함을 만든다

확정된 흐름:

1. 캠지기 **입금 확인** → 캠핏이 제안 발송
2. 크리에이터는 **메일 안 링크**로 크리에이터 포털 진입
3. 포털에서 **수락 / 거절**. 거절 시 **패널티 내용을 화면에 명시**
4. 메일 발송은 **Airtable 자동화**로

**설계 주의 — 링크에 제안 ID를 담지 않는다.**
`/dashboard/proposals/{제안ID}` 로 만들면 URL을 아는 누구나 열 수 있다.
링크는 **목록(`/dashboard/proposals`)으로만** 보내고, 로그인한 본인의 제안만 서버가 골라준다(기존 JWT·IDOR 방어 패턴 재사용).

**남은 개발은 이것 하나다.** 스펙은 있으나 2026-07-15 문서라 v2·표준화 이후 스키마와 대조가 먼저다.

---

## 2. 🔴 오픈은 한 번이 아니라 **두 단계**입니다

```
1단계 · 크리에이터에게 오픈
   배포 → 온보딩 안내 발송 → 프로필 채워짐 → 심사 → 승인
                                                    ↓
2단계 · 캠지기에게 오픈
   승인된 크리에이터가 쌓인 뒤에야 리스트가 쓸모 있음
```

**둘을 같이 열면 캠지기가 빈 리스트를 봅니다.** 첫인상이 "아무도 없네"면 다시 안 들어옵니다.

캠지기측도 같은 말을 했습니다 — *"크리에이터 정보가 모이고 캠지기가 실제로 고를 수 있을 때"*.

**✅ 결정 — 승인 10명.** 10명이 모이면 공지성 글과 함께 크리에이터 오픈.

---

## 3. 🔴 심사를 돌릴 준비가 안 돼 있습니다

전수 심사를 하기로 했는데(v2 결정), 돌릴 사람도 도구도 없습니다.

### 3-1. 심사 담당자와 SLA

화면 두 곳에 **이미 박혀 있습니다.**

> 확인이 끝나면 캠지기에게 보입니다. **보통 1영업일** 걸려요.
> 캠핏 확인 후 캠지기에게 보입니다 **(보통 1영업일)**

**✅ 결정 — 심사는 사장님이 직접.** "1영업일" 문구 유지.

### 3-2. Airtable 심사 뷰가 없습니다

크리에이터 명단 뷰 9개 전수 확인 결과, **심사 관련 뷰 0개**입니다.

```
Grid view · 외부용 · 유튜브/인스타/블로그 등급화 · 크리에이터 등급화 · 영업 자료 3종
```

지금 오픈하면 354명 그리드에서 눈으로 찾아야 합니다. 필요한 건 둘입니다.

⚠️ **Airtable REST API는 뷰 생성을 지원하지 않습니다.** UI에서 직접 만드셔야 합니다.
`크리에이터 명단` → 좌측 뷰 목록 하단 `+` → Grid

| 뷰 이름 | 필터 |
|---|---|
| **심사대기** | `프로필 심사 상태` **is** `심사대기` |
| **재검토 필요** | `지표 재검토 필요` **is** ✓ **AND** `프로필 심사 상태` **is** `승인` |

(대안: 터미널에서 심사 대기 목록을 뽑는 스크립트를 만들 수 있습니다 — 뷰 없이도 됩니다)

### 3-3. 심사 기준이 문서로 없습니다

캠지기측은 만들었습니다 — `camjigi_claude/docs/[ops] 지명형_프로필심사_체크리스트_20260825.md`
(1번 항목: "동반 인원 비어 있으면 승인하지 말 것")

**캠핏 쪽 심사자가 볼 기준도 필요합니다.** 무엇을 보고 승인/반려할지, 반려 사유를 어떻게 쓸지.

---

## 4. 🟡 놓치기 쉬운 것 — 제가 확인하면서 발견한 것들

### 4-1. 심사 결과를 크리에이터가 모릅니다

승인·반려 시 **아무 알림도 안 나갑니다.** 크리에이터가 포털에 다시 들어와야 압니다.

반려일 때 특히 문제입니다 — 고쳐야 할 게 있는데 고치라는 말을 들을 방법이 없습니다.
(반려 사유를 화면에 보여주는 코드는 있습니다. 다시 들어와야 볼 뿐입니다.)

**✅ 결정 — 사장님이 카톡으로 직접 연락.** 알림 자동화는 만들지 않습니다.

### 4-2. 포트폴리오 페이지에 개인정보 수집 고지가 없습니다

이 페이지는 이메일·채널 URL·지표·콘셉트를 받아 **캠지기에게 공개**합니다.
공개 시 *"공개되며, 사실과 다르면 협찬이 무효가 될 수 있어요"* 라는 안내는 있지만,
`/premium-register`에 있는 것 같은 **개인정보 수집·이용 동의 고지는 없습니다.**

**✅ 완료 (커밋 `248dfbb`)** — 공개 신청 버튼 바로 위에 수집 항목·이용 목적·제공 대상·보유 기간 4가지를 고지합니다.
정산 계좌·주민등록번호는 이 화면에서 받지 않는다는 것도 함께 밝힙니다.
⚠️ **문안은 법무 확인 전 초안입니다.**

### 4-3. 승인 후 이미지만 바꾸면 재검토가 안 걸립니다

지표·링크·채널을 고치면 `지표 재검토 필요`가 서는데, **프로필 이미지만 교체하면 아무 플래그도 안 섭니다.**
이미지가 PATCH payload에 없어 서버가 변경을 알 방법이 없기 때문입니다.

**✅ 결정 — 이미지 교체는 자유. 재검토 대상으로 만들지 않습니다.** 코드 변경 없음(현재 동작이 이미 그렇습니다).

### 4-4. 포인터 클릭 검증이 안 됐습니다

Task 15 12개 항목은 전부 통과했지만, **JS로 React 핸들러를 직접 발화**시킨 결과입니다.
브라우저 자동화의 포인터 클릭이 이 환경에서 죽어 있었습니다(원인: `document.hidden=true`로 rAF 정지 — 캠지기측 진단).

상태 변화·저장·서버 검증은 실제로 태웠지만, **"요소가 가려져 실제 손가락으로는 안 눌리는"** 종류는 못 잡습니다.
375px에서 뷰포트 밖 요소 0개·가로 스크롤 없음은 확인해 위험을 줄였을 뿐입니다.

**✅ 완료 (2026-08-25)** — 실제 Chrome(Browser 2)에서 포인터 클릭으로 재검증했습니다.
로그인(채널 선택 → 로그인 버튼) → 기한 21일 칩 → 저장까지 실제 클릭으로 눌렀고, **Airtable에 `21`이 기록된 것**까지 확인.

배운 것: 프리뷰 페인에서 두 번(08-11·08-25) 죽었던 클릭이 **실제 Chrome에선 정상**입니다 — 앱 문제가 아니라 페인 문제입니다.
단 **한글 타이핑은 실제 Chrome에서도 자동화로 안 들어갑니다**(CDP가 한글 조합을 못 만듦). 사람은 IME로 치므로 무관합니다.

---

## 5. 🔴 Airtable 자동화 — 지명형 3개가 **전부 미배포**

베이스 자동화 36개를 전수 조회했습니다. 지명형 관련은 3개고, **셋 다 미배포**입니다.

| 자동화 | 하는 일 | 상태 |
|---|---|---|
| 지명형 협찬 제안 접수 시점<br>`wflDxUWuoUuRPTBKs` | 제안 생성 시 캠지기에게 선입금 안내 메일 + 운영 슬랙 | 🔴 미배포 |
| 지명형 협찬 제안서 크리에이터 발송<br>`wflZmG34NnNVuZiHw` | 입금 확인 후 크리에이터에게 제안서 메일, 상태 → `크리에이터확인중` | 🔴 미배포 |
| 지명형 선입금 기한 만료 처리<br>`wflxcs0loaY3XqFnz` | 매시간, 기한 지난 `선입금대기` → `선입금만료` | 🔴 미배포 |

**셋 다 만들어져 있고 배포만 안 됐습니다.** Airtable에서 토글 하나입니다.

**✅ 결정 — 오픈 시점에 배포. 그 전에 테스트를 한 번 돌려본다.**
- 메일 수신: `keyluke97@nextedition.co.kr`
- 범위: **Airtable 레코드만.** 캠핏 프로덕션에 테스트 캠핑장을 만들지 않습니다 — 제안 접수·메일·상태 이동이 전부 Airtable 안에서 끝나 **노출 위험 0**
- 크리에이터 테스트 계정은 기존 `recmH8fnxcO7r3OUN` 사용

⚠️ **배포하는 순간부터 실제 메일이 나갑니다.** 테스트 레코드(§6)를 지운 뒤에 켜세요. 순서가 바뀌면 목업 계정 앞으로 메일이 나갑니다.

### 그런데 없는 자동화가 있습니다 — 이게 §1과 합쳐지면 심각합니다

**① 크리에이터 응답 기한 만료 → 자동 확정** — **없습니다**

화면은 *"응답이 없으면 수락한 것으로 자동 확정됩니다"* 라고 약속합니다.
그런데 위 3개 중 **어느 것도 `크리에이터확인중`을 다음 상태로 옮기지 않습니다.** 그 일을 하는 자동화가 아예 없습니다.

§1(제안 수신함 없음)과 겹치면 이렇게 됩니다.

```
캠지기 선입금  →  제안서 메일  →  상태 = 크리에이터확인중
                                        ↓
                        수락할 곳 없음 · 거절할 곳 없음 · 자동 확정도 없음
                                        ↓
                              영원히 크리에이터확인중
```

**캠지기 돈은 이미 들어와 있는데 제안이 어디로도 못 갑니다.** 앞서 "무응답이면 자동 수락된다"고 적었는데, 자동화를 확인해보니 **자동 수락조차 안 됩니다.** 그냥 멈춥니다.

**② 프로필 심사 대기 알림** — 없습니다

크리에이터가 공개 신청하면 `심사대기`가 되는데 **아무도 모릅니다.** 슬랙 알림이 없으면 §3-1의 "1영업일"을 지킬 방법이 없습니다.
(프리미엄 쪽엔 같은 패턴이 이미 있습니다 — `프리미엄 협찬 일간 알림봇`, 매일 14시 cron)

**③ 심사 결과 → 크리에이터 알림** — 없습니다 (§4-1과 같은 건)

### 곁다리 — 깨진 자동화 1건

`인플루언서 글카펜 자동화`(`wflKElvXjsf6ZOK75`)가 **`deploymentStatus: error`** 상태입니다.
지명형과 무관하지만 전수 조회하다 눈에 띄어 적습니다.

---

## 6. 오픈 직전에 반드시 지울 것

테스트/목업 계정 **6건**입니다. 그중 **4건이 공개 ON** 상태라 배포되면 그대로 리스트에 뜹니다.

```
rec4HXSY9QQEWdeOz  공개 ON   [TEST] 목업_블로그캠퍼
rec8NaVqvHCygKj4J  공개 ON   [TEST] 목업_캠핑러버
reclAAEgdIykwKxln  공개 ON   [TEST] 목업_남도캠핑TV
recsJ8gXBJeUdFeTZ  공개 ON   [TEST] 목업_휴먼ON(노출금지)
rech2BU4iQmLg2oKc  공개 off  [TEST] 목업_비공개(노출금지)
recmH8fnxcO7r3OUN  공개 off  [TEST]지명형확인_삭제예정   ← 심사=승인
```

이름에 `노출금지`가 붙은 것까지 공개 ON입니다.

**✅ 결정 — 사장님이 오픈 직전에 직접 삭제.**

⚠️ `recmH8fnxcO7r3OUN`은 **§5 자동화 테스트에도 쓸 계정**입니다(`7777`). 그 테스트까지 끝낸 뒤 지우세요.

---

## 7. 순서대로 할 일

### 지금 바로
- [ ] **Task 15 Step 4 — 직접 확인** (dev 서버 :3000 / `[TEST]지명형확인_삭제예정` / `7777`)
- [ ] Airtable **심사 뷰 2개** 생성 (§3-2)
- [ ] PR [#14](https://github.com/keyluke97-ui/camfit_creator/pull/14) 정리 — Draft로 5주 정지 중

### 결정이 필요한 것
- [ ] **§1+§5 — 제안 흐름**: A(수신함 만들고 오픈) / B(1단계만 오픈) / C(문구 수정) 중 택
      ⚠️ §5에서 자동 확정 자동화도 없는 것이 확인돼, C를 골라도 제안이 멈춥니다
- [ ] **§2 — 2단계 전환 기준**: 승인 몇 명부터 캠지기에게 열 것인가
- [ ] **§3-1 — 심사 담당자와 SLA**: "1영업일"을 지킬 것인가, 문구를 고칠 것인가
- [ ] **§4-1 — 심사 결과 알림**: 붙일 것인가, 수동 연락인가
- [ ] **§4-2 — 개인정보 고지**: 법무 확인
- [ ] **§4-3 — 이미지 재검토**: 막을 것인가 둘 것인가

### 배포 직전
- [ ] **지명형 자동화 3개 배포** (§5) — 테스트 레코드 삭제 **후**
- [ ] 심사 대기 슬랙 알림 자동화 신설 (§5-②)
- [ ] 심사 기준 문서 (캠핏 쪽)
- [ ] 크리에이터 온보딩 안내 문안 + 발송 방법
- [ ] 테스트 레코드 6건 삭제 (§6) — **자동화 배포보다 먼저**
- [ ] 브랜치 푸시 → PR → 머지 → **Vercel 빌드 성공까지 확인** (머지 = 배포 아님)
- [ ] 배포 시점을 캠지기측과 맞추기 (합의됨)

### 오픈 후 첫 주 지켜볼 것
- [ ] **동반 인원이 실제로 저장되는지** — 안 쌓이면 조용히 실패하는 종류라 발견이 늦습니다
- [ ] 심사 대기가 1영업일 안에 처리되는지
- [ ] 공개 신청이 400으로 막히는 사례 (이제 이유가 화면에 나옵니다)

---

## 8. 이미 해결된 것 (참고)

오해 없도록, 한때 위험으로 적었다가 닫힌 것들입니다.

| 항목 | 결과 |
|---|---|
| "배포하면 공개 신청 전원 차단" | **틀린 진단이었음.** 게이트는 payload를 봄 |
| "레거시가 심사 없이 리스트에 뜬다" | **성립 안 함.** 캠지기 리스트에 승인 게이트가 한 겹 더 있음 (353명 전원 심사 빈 값) |
| 동반 인원 백필 | **반려.** 넣을 값이 없음 — 지어낸 자기신고가 됨 |
| 위반 시 이유를 알 수 없음 | **해결.** 11종 전부 한국어 문장 |
| 포털·캠지기가 다른 기한 렌더 | **해결.** 화이트리스트 일원화 |
| 조사 오류 (`여행로`) | **해결.** 12종 중 6종이 틀리던 자리 |
