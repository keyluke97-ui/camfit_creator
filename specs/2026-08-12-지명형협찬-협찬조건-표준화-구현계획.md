# 협찬 조건 표준화 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 협찬 조건을 크리에이터가 채워야 존재하는 것에서, 캠핏이 표준을 정해두고 다른 사람만 고치는 구조로 바꾼다.

**Architecture:** 표준값은 Airtable이 아니라 `lib/constants.ts` 상수에 둔다. 크리에이터가 안 고치면 Airtable 필드는 빈 채로 남고 화면이 상수를 렌더한다. 표준 문구 생성은 `lib/sponsorshipTerms.ts` 순수 함수 한 곳에 모아 포털·캠지기·제안서가 같은 문장을 쓰게 한다.

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · Tailwind 4 · Airtable REST (`npm airtable`)

**설계 문서:** `specs/2026-08-12-지명형협찬-협찬조건-표준화.md` (이하 "스펙")

**브랜치:** `feat/jimyeong-1a-v2` (v2 Task 3~18 완료 상태에서 이어감)

---

## ⚠️ 이 레포의 검증 방식 — 테스트 러너 없음

jest/vitest가 없고 테스트 파일도 0개다. **테스트 프레임워크를 새로 도입하지 않는다** (v2 스펙에서 확정된 결정). 대신 이 레포가 쓰는 4단 검증을 쓴다:

| 단계 | 명령 | 무엇을 잡나 |
|---|---|---|
| 1. 순수 로직 | `npx tsx tools/jimyeong/verify-rules.ts` | 판정 함수의 입출력 |
| 2. 타입 | `npx tsc --noEmit` | 필드명 오타·타입 불일치 |
| 3. 린트 | `npx eslint components app lib types` | 컨벤션 위반 |
| 4. 브라우저 | dev 서버 + 테스트 계정 | 실제 렌더·저장·Airtable 반영 |

**TDD는 1단계에서 돈다.** 케이스를 먼저 추가해 실패를 확인하고, 구현한 뒤 통과를 확인한다. 현재 기준선은 **29 passed, 0 failed**.

`npx eslint`가 보고하는 **기존 오류 6건**(`app/api/applications/{checkin,my,status}/route.ts`의 `any`, `CheckinModal` 이스케이프, `SearchableSelect` effect)은 이 작업 범위 밖이다. **신규·수정 파일에서 0 error**면 통과다.

---

## 파일 구조

### 새로 만드는 파일

| 경로 | 책임 |
|---|---|
| `lib/sponsorshipTerms.ts` | 표준 조건을 사람이 읽는 한 줄로 만드는 순수 함수. Airtable·React 의존 없음 |
| `components/ChannelConceptField.tsx` | 채널콘셉트 12종 칩 선택 + 운영자 폴백 안내 |

### 수정하는 파일

| 경로 | 변경 |
|---|---|
| `lib/constants.ts` | 표준 상수 5개 추가 |
| `types/index.ts` | `CreatorProfile`·`CreatorProfileUpdate`에 5필드 추가 |
| `lib/creatorProfileRules.ts` | 검증 3종 + 정규화 1종 + 게이트·완성도 확장 |
| `lib/airtable.ts` | `getCreatorProfile` 읽기 / `updateCreatorProfile` 쓰기 |
| `app/api/creator/profile/route.ts` | 방어적 파싱에 5필드 추가 |
| `components/ContentFormatFields.tsx` | 표준 문구 배너 + 업로드 기한 3택 |
| `components/AcceptanceConditionFields.tsx` | 동반 인원 · 반려동물 · 드론 |
| `components/PortfolioEditForm.tsx` | 위 3개 배선 + payload 확장 |
| `tools/jimyeong/verify-rules.ts` | 케이스 추가 (29 → 62) |

### 삭제하는 파일

없음.

---

## Task 0: Airtable 신규 필드 5개 — 🛑 선행 조건

**Base** `appEGM6qarNr9M7HN` / **Table** `크리에이터 명단` `tblkuPln7nquA3dLA`

- [ ] **Step 1: 캠지기측에 필드 생성 요청**

| 필드명 | 타입 | 옵션 |
|---|---|---|
| `업로드 기한(일)` | number (정수, precision 0) | — |
| `동반 인원` | number (정수, precision 0) | — |
| `반려동물 동반` | checkbox | — |
| `드론 촬영` | checkbox | — |
| `채널콘셉트(자기신고)` | multipleSelects | 아래 12종 |

`채널콘셉트(자기신고)` 옵션 — 기존 `채널콘셉트` 필드에서 **문자열 그대로 복사**한다:

```
캠핑, 등산, 여행, 가족, 커플, 솔로, 반려동물, 차박, 백패킹, 장비리뷰, 캠핑요리, 낚시
```

- [ ] **Step 2: 생성 후 실측 검증**

이름만 맞춰보고 끝내지 말고 **테스트 레코드에 실제로 써서** 확인한다. Airtable은 없는 필드에 써도 조용히 무시하는 경우가 있어 무증상 실패가 난다.

```bash
TOKEN=$(grep AIRTABLE_ACCESS_TOKEN .env.local | cut -d= -f2)
curl -s "https://api.airtable.com/v0/meta/bases/appEGM6qarNr9M7HN/tables" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import json,sys
for t in json.load(sys.stdin)['tables']:
    if t['id']=='tblkuPln7nquA3dLA':
        want={'업로드 기한(일)','동반 인원','반려동물 동반','드론 촬영','채널콘셉트(자기신고)'}
        have={f['name'] for f in t['fields']}
        print('없는 필드:', want-have or '(없음 — 전부 생성됨)')
        for f in t['fields']:
            if f['name']=='채널콘셉트(자기신고)':
                print('옵션:', [o['name'] for o in f.get('options',{}).get('choices',[])])
"
```

기대: `없는 필드: (없음 — 전부 생성됨)` + 옵션 12종 출력

> 🛑 **Task 7~9(데이터 레이어)와 Task 15(브라우저)는 이 Task 없이 진행할 수 없다.** Task 1~6(순수 로직)과 Task 10~13(UI)은 Airtable 없이도 만들 수 있으므로 먼저 진행해도 된다.

---

# Phase A — 순수 로직

## Task 1: `lib/constants.ts` — 표준 상수

**Files:**
- Modify: `lib/constants.ts` (파일 끝에 추가)

- [ ] **Step 1: 상수 추가**

`CHANNEL_METRIC_LABELS` 정의 **아래**에 붙인다:

```ts
// ─────────────────────────────────────────────────────────────────────────
// 협찬 조건 표준 (2026-08-12) — 스펙 §3
// ⚠️ 표준값은 Airtable에 저장하지 않는다. 빈 값이 곧 "표준 적용 중"이다.
//    camfit-premium/netlify/functions/shared-constants.js와 동일값 유지.
// ─────────────────────────────────────────────────────────────────────────

/**
 * 업로드 기한 표준 — 체크아웃(입실 + 1박) 후 14일.
 * ⚠️ tools/content-followup/overdue.cjs의 GRACE_DAYS와 반드시 같은 값이어야 한다.
 *    두 곳이 어긋나면 독촉 도구가 뽑는 대상과 화면 안내가 달라진다.
 */
export const UPLOAD_DEADLINE_DEFAULT_DAYS = 14;

/** 예외로 고를 수 있는 기한. "더 길게"만 둔다 — 짧게 걸었다 못 지키면 그게 곧 분쟁(스펙 E4) */
export const UPLOAD_DEADLINE_OPTIONS: number[] = [21, 30];

/** 동반 인원 허용 범위. 상한 10은 캠핑 사이트 정원 현실 — 없으면 오타(999)가 캠지기 카드에 뜬다 */
export const COMPANION_MIN = 1;
export const COMPANION_MAX = 10;

/**
 * 채널콘셉트 12종 — 기존 `채널콘셉트`(운영자 관리) 필드 옵션과 문자열이 동일해야 한다.
 * 자기신고 값과 운영자 값을 같은 자리에 렌더하므로 어긋나면 안 된다.
 */
export const CHANNEL_CONCEPTS: string[] = [
    '캠핑', '등산', '여행', '가족', '커플', '솔로',
    '반려동물', '차박', '백패킹', '장비리뷰', '캠핑요리', '낚시',
];
```

- [ ] **Step 2: 타입검사**

```bash
npx tsc --noEmit
```
기대: 오류 0

- [ ] **Step 3: `overdue.cjs`와 값이 같은지 확인**

```bash
grep -n "GRACE_DAYS" tools/content-followup/overdue.cjs | head -2
grep -n "UPLOAD_DEADLINE_DEFAULT_DAYS" lib/constants.ts
```
기대: 양쪽 모두 `14`

- [ ] **Step 4: 커밋**

```bash
git add lib/constants.ts
git commit -m "feat(jimyeong): 협찬 조건 표준 상수 (Task 1)"
```

---

## Task 2: `types/index.ts` — 타입 확장

**Files:**
- Modify: `types/index.ts` (`CreatorProfile`, `CreatorProfileUpdate`)
- Modify: `tools/jimyeong/verify-rules.ts` (`base` 객체 — 필수 필드가 늘어 tsc가 깨진다)

- [ ] **Step 1: `CreatorProfile`에 필드 추가**

`creatorEmail: string;` 줄 **아래**, `// CHANGED: 1a-v2 §4 — 심사` 주석 **위**에 넣는다:

```ts
  // CHANGED: 2026-08-12 협찬 조건 표준화 — 빈 값 = 표준 적용 중(스펙 E1/E2)
  uploadDeadlineDays: number | null;   // null이면 표준 14일
  companions: number;                  // 동반 인원. 0이면 미입력
  petAllowed: boolean;                 // 반려동물 동반. false가 표준
  droneUsed: boolean;                  // 드론 촬영. false가 표준
  channelConcepts: string[];           // 자기신고. 크리에이터가 고른 것
  channelConceptsFallback: string[];   // 운영자 `채널콘셉트`. 읽기 전용 — 자기신고가 비었을 때만 표시
```

- [ ] **Step 2: `CreatorProfileUpdate`에 필드 추가**

`creatorEmail: string;` 줄 **아래**에 넣는다. **`channelConceptsFallback`은 넣지 않는다** — 운영자 필드는 크리에이터가 못 바꾼다:

```ts
  // CHANGED: 2026-08-12 협찬 조건 표준화
  uploadDeadlineDays: number | null;
  companions: number;
  petAllowed: boolean;
  droneUsed: boolean;
  channelConcepts: string[];           // 자기신고만. 운영자 `채널콘셉트`에는 절대 쓰지 않는다
```

- [ ] **Step 3: `verify-rules.ts`의 `base` 객체 보강**

`creatorEmail: 'a@b.com',` 줄 **아래**에 추가한다:

```ts
    uploadDeadlineDays: null,
    companions: 2,
    petAllowed: false,
    droneUsed: false,
    channelConcepts: ['캠핑'],
```

- [ ] **Step 4: 타입검사 + 기존 케이스 회귀 확인**

```bash
npx tsc --noEmit && npx tsx tools/jimyeong/verify-rules.ts
```
기대: tsc 오류 0 / `29 passed, 0 failed`

- [ ] **Step 5: 커밋**

```bash
git add types/index.ts tools/jimyeong/verify-rules.ts
git commit -m "feat(jimyeong): 협찬 조건 타입 확장 (Task 2)"
```

---

## Task 3: `buildDeliverableSummary` — 산출물 + 기한 문구

**Files:**
- Create: `lib/sponsorshipTerms.ts`
- Modify: `tools/jimyeong/verify-rules.ts`

이 함수가 **B안 전체의 단일 실패점**이다. 포털·캠지기 카드·제안서가 전부 이 문장을 쓴다.

- [ ] **Step 1: 실패하는 케이스를 먼저 추가**

`verify-rules.ts` 상단 import에 추가:

```ts
import { buildDeliverableSummary } from '../../lib/sponsorshipTerms';
```

`// ── isValidEmail ──` 블록 **위**에 추가:

```ts
// ── buildDeliverableSummary (표준 문구 단일 출처) ──
check(
    '형식 2개 + 표준 기한',
    buildDeliverableSummary(['유튜브 롱폼', '인스타 릴스'], null),
    '유튜브 롱폼 1편 · 인스타 릴스 1편 — 체크아웃 후 14일 안에 업로드'
);
check(
    '형식 1개 + 예외 기한',
    buildDeliverableSummary(['블로그 포스팅'], 21),
    '블로그 포스팅 1편 — 체크아웃 후 21일 안에 업로드'
);
check(
    '형식 미선택 → 기한만',
    buildDeliverableSummary([], null),
    '체크아웃 후 14일 안에 업로드'
);
check('형식 미선택 + 예외 기한', buildDeliverableSummary([], 30), '체크아웃 후 30일 안에 업로드');
// 표준과 같은 값(14)이 실수로 들어와도 표준 문구가 나와야 한다
check('기한 14가 들어와도 표준 문구', buildDeliverableSummary([], 14), '체크아웃 후 14일 안에 업로드');
```

- [ ] **Step 2: 실패 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: 모듈이 없어 **에러로 종료**한다 (`Cannot find module '../../lib/sponsorshipTerms'`)

- [ ] **Step 3: 구현**

`lib/sponsorshipTerms.ts` 신규 생성:

```ts
// sponsorshipTerms.ts - 협찬 조건 표준을 사람이 읽는 한 줄로 만든다 (2026-08-12 스펙 §6.2)
// 포털 화면·캠지기 카드·제안서가 같은 문장을 써야 한다. 두 벌이면 "듣던 것과 다르다"가 그대로 분쟁이 된다.
// ⚠️ Airtable·React에 의존하지 않는다 (tsx로 직접 돌려 검증할 수 있어야 한다).

import { UPLOAD_DEADLINE_DEFAULT_DAYS } from './constants';

/**
 * 산출물 + 업로드 기한을 한 줄로.
 * @param contentFormats 선택한 콘텐츠 형식. 비어 있으면 기한만 반환한다
 * @param uploadDeadlineDays null이거나 표준과 같으면 표준 일수를 쓴다
 */
export function buildDeliverableSummary(
    contentFormats: string[],
    uploadDeadlineDays: number | null
): string {
    const days = uploadDeadlineDays && uploadDeadlineDays !== UPLOAD_DEADLINE_DEFAULT_DAYS
        ? uploadDeadlineDays
        : UPLOAD_DEADLINE_DEFAULT_DAYS;
    const deadline = `체크아웃 후 ${days}일 안에 업로드`;

    if (contentFormats.length === 0) return deadline;

    // 표준은 "형식마다 1편"이다 (스펙 E5 — 편수 예외 필드를 만들지 않는다)
    const items = contentFormats.map((format) => `${format} 1편`).join(' · ');
    return `${items} — ${deadline}`;
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: `34 passed, 0 failed`

- [ ] **Step 5: 커밋**

```bash
git add lib/sponsorshipTerms.ts tools/jimyeong/verify-rules.ts
git commit -m "feat(jimyeong): 산출물·기한 표준 문구 (Task 3)"
```

---

## Task 4: `buildVisitConditionSummary` — 현장 조건 문구

**Files:**
- Modify: `lib/sponsorshipTerms.ts`
- Modify: `tools/jimyeong/verify-rules.ts`

- [ ] **Step 1: 실패하는 케이스를 먼저 추가**

`verify-rules.ts`의 import를 고친다:

```ts
import { buildDeliverableSummary, buildVisitConditionSummary } from '../../lib/sponsorshipTerms';
```

`buildDeliverableSummary` 케이스 **아래**에 추가:

```ts
// ── buildVisitConditionSummary (표준과 같은 항목은 생략) ──
check('표준 그대로', buildVisitConditionSummary(2, false, false), '2인 방문');
check('전부 예외', buildVisitConditionSummary(4, true, true), '4인 방문 · 반려동물 동반 · 드론 촬영');
check('반려동물만', buildVisitConditionSummary(1, true, false), '1인 방문 · 반려동물 동반');
check('드론만', buildVisitConditionSummary(3, false, true), '3인 방문 · 드론 촬영');
check('인원 미입력', buildVisitConditionSummary(0, false, false), '');
// 인원이 없어도 예외 항목은 알려야 한다 — 현장에서 알면 늦는 정보다
check('인원 미입력 + 반려동물', buildVisitConditionSummary(0, true, false), '반려동물 동반');
```

- [ ] **Step 2: 실패 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: `Cannot find name 'buildVisitConditionSummary'` 로 실패

- [ ] **Step 3: 구현**

`lib/sponsorshipTerms.ts` 파일 끝에 추가:

```ts
/**
 * 현장 조건 한 줄. 표준과 같은 항목(반려동물 없음·드론 없음)은 생략한다.
 * 동반 인원이 0(미입력)이어도 예외 항목은 표시한다 — 현장에서 알면 늦는 정보라 빠뜨리면 안 된다.
 */
export function buildVisitConditionSummary(
    companions: number,
    petAllowed: boolean,
    droneUsed: boolean
): string {
    const parts: string[] = [];
    if (companions > 0) parts.push(`${companions}인 방문`);
    if (petAllowed) parts.push('반려동물 동반');
    if (droneUsed) parts.push('드론 촬영');
    return parts.join(' · ');
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: `40 passed, 0 failed`

- [ ] **Step 5: 커밋**

```bash
git add lib/sponsorshipTerms.ts tools/jimyeong/verify-rules.ts
git commit -m "feat(jimyeong): 현장 조건 표준 문구 (Task 4)"
```

---

## Task 5: 검증 3종 + 기한 정규화

**Files:**
- Modify: `lib/creatorProfileRules.ts`
- Modify: `tools/jimyeong/verify-rules.ts`

- [ ] **Step 1: 실패하는 케이스를 먼저 추가**

`verify-rules.ts` import를 고친다:

```ts
import {
    validateChannelPayload, collectMissingForPublish, needsReReview, isValidEmail,
    isFormatAvailable, pruneContentFormats, normalizeUploadDeadline,
} from '../../lib/creatorProfileRules';
```

`buildVisitConditionSummary` 케이스 **아래**에 추가:

```ts
// ── normalizeUploadDeadline (표준과 같은 값은 null로 — 스펙 §9) ──
check('표준값 14 → null', normalizeUploadDeadline(14), null);
check('null → null', normalizeUploadDeadline(null), null);
check('0 → null', normalizeUploadDeadline(0), null);
check('예외 21 유지', normalizeUploadDeadline(21), 21);
check('예외 30 유지', normalizeUploadDeadline(30), 30);
check('허용 밖 7은 그대로 (검증이 잡는다)', normalizeUploadDeadline(7), 7);

// ── validateChannelPayload 신규 위반 3종 ──
check('기한 21 통과', validateChannelPayload({ ...base, uploadDeadlineDays: 21 }), null);
check('기한 null 통과', validateChannelPayload({ ...base, uploadDeadlineDays: null }), null);
check('기한 7 → 위반', validateChannelPayload({ ...base, uploadDeadlineDays: 7 }), 'UPLOAD_DEADLINE_INVALID');
check('기한 14 → 위반(정규화 전이면 안 들어와야 한다)', validateChannelPayload({ ...base, uploadDeadlineDays: 14 }), 'UPLOAD_DEADLINE_INVALID');

check('인원 0(미입력) 통과', validateChannelPayload({ ...base, companions: 0 }), null);
check('인원 10 통과', validateChannelPayload({ ...base, companions: 10 }), null);
check('인원 11 → 위반', validateChannelPayload({ ...base, companions: 11 }), 'COMPANION_INVALID');
check('인원 -1 → 위반', validateChannelPayload({ ...base, companions: -1 }), 'COMPANION_INVALID');
check('인원 소수 → 위반', validateChannelPayload({ ...base, companions: 2.5 }), 'COMPANION_INVALID');

check('콘셉트 빈 배열 통과', validateChannelPayload({ ...base, channelConcepts: [] }), null);
check('콘셉트 2개 통과', validateChannelPayload({ ...base, channelConcepts: ['캠핑', '차박'] }), null);
check('콘셉트 화이트리스트 밖 → 위반', validateChannelPayload({ ...base, channelConcepts: ['등산', '서핑'] }), 'CONCEPT_UNKNOWN');
```

- [ ] **Step 2: 실패 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: `Cannot find name 'normalizeUploadDeadline'` 로 실패

- [ ] **Step 3: 구현**

`lib/creatorProfileRules.ts` — import를 고친다:

```ts
import {
    CHANNEL_TYPES,
    REPRESENTATIVE_CHANNELS,
    CONTENT_FORMATS,
    CONTENT_FORMAT_CHANNEL,
    UPLOAD_DEADLINE_DEFAULT_DAYS,
    UPLOAD_DEADLINE_OPTIONS,
    COMPANION_MIN,
    COMPANION_MAX,
    CHANNEL_CONCEPTS,
} from './constants';
```

`ChannelViolation` 유니온에 3종을 추가한다:

```ts
export type ChannelViolation =
    | 'CHANNEL_REQUIRED'
    | 'CHANNEL_UNKNOWN'
    | 'REPRESENTATIVE_UNKNOWN'
    | 'REPRESENTATIVE_NOT_OWNED'
    | 'FORMAT_UNKNOWN'
    | 'FORMAT_CHANNEL_MISMATCH'
    | 'EMAIL_INVALID'
    | 'METRIC_INVALID'
    // CHANGED: 2026-08-12 협찬 조건 표준화
    | 'UPLOAD_DEADLINE_INVALID'
    | 'COMPANION_INVALID'
    | 'CONCEPT_UNKNOWN';
```

`isValidEmail` 함수 **아래**에 정규화 함수를 추가한다:

```ts
/**
 * 업로드 기한을 저장 가능한 형태로 정규화한다.
 * 표준과 같은 값(14)이나 0/null은 전부 null로 눕힌다 — 빈 값이 곧 "표준 적용 중"이라는
 * 불변식을 지켜야, 나중에 표준을 15로 바꿔도 14가 박힌 사람만 남는 일이 안 생긴다(스펙 §9).
 * 허용 밖 값(예: 7)은 고치지 않고 그대로 돌려준다 — 판정은 validateChannelPayload가 한다.
 */
export function normalizeUploadDeadline(value: number | null): number | null {
    if (!value || value === UPLOAD_DEADLINE_DEFAULT_DAYS) return null;
    return value;
}
```

`validateChannelPayload`의 `return null;` **바로 위**에 검증 3종을 추가한다:

```ts
    // 규칙 8 — 업로드 기한은 표준(null) 또는 허용 예외뿐 (스펙 E4)
    const deadline = payload.uploadDeadlineDays;
    if (deadline !== null && !UPLOAD_DEADLINE_OPTIONS.includes(deadline)) {
        return 'UPLOAD_DEADLINE_INVALID';
    }

    // 규칙 9 — 동반 인원. 0은 미입력이라 허용한다(저장은 되고 공개만 막힌다)
    const companions = payload.companions;
    if (!Number.isInteger(companions) || companions < 0) return 'COMPANION_INVALID';
    if (companions > 0 && (companions < COMPANION_MIN || companions > COMPANION_MAX)) {
        return 'COMPANION_INVALID';
    }

    // 규칙 10 — 채널콘셉트 화이트리스트
    if ((payload.channelConcepts || []).some((concept) => !CHANNEL_CONCEPTS.includes(concept))) {
        return 'CONCEPT_UNKNOWN';
    }
```

- [ ] **Step 4: 통과 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts && npx tsc --noEmit
```
기대: `58 passed, 0 failed` / tsc 오류 0

- [ ] **Step 5: 커밋**

```bash
git add lib/creatorProfileRules.ts tools/jimyeong/verify-rules.ts
git commit -m "feat(jimyeong): 기한·인원·콘셉트 검증 (Task 5)"
```

---

## Task 6: 공개 게이트 + 완성도 확장

**Files:**
- Modify: `lib/creatorProfileRules.ts`
- Modify: `tools/jimyeong/verify-rules.ts`

- [ ] **Step 1: 실패하는 케이스를 먼저 추가**

Task 5에서 추가한 블록 **아래**에 넣는다:

```ts
// ── 공개 게이트에 동반 인원 (스펙 E3) ──
check(
    '인원 미입력 → 공개 못 함',
    collectMissingForPublish({ ...base, companions: 0 }, true, true),
    ['동반 인원']
);
check('인원 입력 → 누락 없음', collectMissingForPublish({ ...base, companions: 2 }, true, true), []);
// 업로드 기한·반려동물·드론은 표준이 있으므로 게이트에 없어야 한다
check(
    '기한 미입력은 공개를 막지 않는다',
    collectMissingForPublish({ ...base, uploadDeadlineDays: null }, true, true),
    []
);

// ── 신규 5필드는 재검토 트리거가 아니다 (스펙 E7) ──
// 콘셉트 변경으로 재검토 큐를 채우면 진짜 지표 변경이 그 안에 묻힌다.
check(
    '콘셉트만 변경 → 재검토 없음',
    needsReReview(before, { ...base, channelConcepts: ['낚시', '백패킹'] }),
    false
);
check(
    '현장 조건만 변경 → 재검토 없음',
    needsReReview(before, { ...base, companions: 6, petAllowed: true, droneUsed: true }),
    false
);
check(
    '업로드 기한만 변경 → 재검토 없음',
    needsReReview(before, { ...base, uploadDeadlineDays: 30 }),
    false
);
```

> 이 블록은 파일에서 `before` 객체가 정의된 **뒤**에 와야 한다. `needsReReview` 기존 케이스들 바로 아래에 붙여라.

- [ ] **Step 2: 실패 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts
```
기대: `FAIL 인원 미입력 → 공개 못 함` — expected `["동반 인원"]`, actual `[]`

- [ ] **Step 3: 구현**

`collectMissingForPublish`의 `if (!payload.creatorEmail) missing.push('크리에이터 이메일');` **아래**에 추가:

```ts
    // CHANGED: 2026-08-12 — 표준으로 못 덮는 유일한 항목(스펙 E3).
    // 사이트 정원은 물리적 제약이라, 표준값을 두면 책임 소재만 확정되고 그날 현장은 그대로 터진다.
    if (!(payload.companions > 0)) missing.push('동반 인원');
```

`computeCompletion`의 `items` 배열에서 `{ label: '정산 정보', filled: hasPremium },` **아래**에 추가:

```ts
        { label: '동반 인원', filled: payload.companions > 0 },
        // 자기신고가 없어도 운영자 값이 있으면 캠지기 카드가 채워지므로 채운 것으로 센다.
        // hasConceptFallback은 호출자가 레코드에서 읽어 넘긴다.
        { label: '채널콘셉트', filled: (payload.channelConcepts || []).length > 0 || hasConceptFallback },
```

> **`업로드 기한`·`반려동물`·`드론`은 완성도 항목에 넣지 않는다.** 표준을 쓰는 게 정상인데 미입력을 미완성으로 세면, 완성도 바가 사실상 "표준을 벗어나라"고 압박하게 된다.

`computeCompletion` 시그니처에 인자를 하나 더 받는다:

```ts
export function computeCompletion(
    payload: CreatorProfileUpdate,
    hasImage: boolean,
    hasPremium: boolean,
    hasConceptFallback: boolean = false
): { percent: number; nextHint: string } {
```

기본값 `false`를 둔 이유 — 기존 호출부 2곳(`PortfolioEditForm`)이 인자 3개로 호출 중이라, 기본값이 없으면 이 Task에서 tsc가 깨진다. Task 13에서 실제 값을 넘기도록 고친다.

- [ ] **Step 4: 통과 확인**

```bash
npx tsx tools/jimyeong/verify-rules.ts && npx tsc --noEmit
```
기대: `62 passed, 0 failed` / tsc 오류 0

- [ ] **Step 5: 커밋**

```bash
git add lib/creatorProfileRules.ts tools/jimyeong/verify-rules.ts
git commit -m "feat(jimyeong): 공개 게이트에 동반 인원 추가 (Task 6)"
```

---

# Phase B — 데이터 레이어

> 🛑 **Task 0(Airtable 필드 생성) 완료 후 진행한다.**

## Task 7: `getCreatorProfile` — 읽기 + 콘셉트 폴백

**Files:**
- Modify: `lib/airtable.ts` (`getCreatorProfile` 반환 객체, 1470번대)

- [ ] **Step 1: 반환 객체에 5필드 추가**

`creatorEmail: (record.get('크리에이터 이메일') as string) || '',` 줄 **아래**에 추가:

```ts
            // CHANGED: 2026-08-12 협찬 조건 표준화 — 빈 값은 그대로 빈 값으로 둔다.
            // 표준값을 여기서 채워 넣으면 "빈 값 = 표준 적용 중" 불변식이 깨지고,
            // 저장 시 그 값이 Airtable에 박혀 표준을 바꿔도 이 사람만 옛 값으로 남는다.
            uploadDeadlineDays: (record.get('업로드 기한(일)') as number) || null,
            companions: (record.get('동반 인원') as number) || 0,
            petAllowed: record.get('반려동물 동반') === true,
            droneUsed: record.get('드론 촬영') === true,
            channelConcepts: (record.get('채널콘셉트(자기신고)') as string[]) || [],
            // 운영자 관리 필드. 353명 중 170명(48%)에 값이 있다 — 자기신고가 비면 이걸 보여준다
            channelConceptsFallback: (record.get('채널콘셉트') as string[]) || [],
```

- [ ] **Step 2: 타입검사**

```bash
npx tsc --noEmit
```
기대: 오류 0

- [ ] **Step 3: 실제 레코드로 읽기 확인**

테스트 계정 `recmH8fnxcO7r3OUN`로 확인한다:

```bash
TOKEN=$(grep AIRTABLE_ACCESS_TOKEN .env.local | cut -d= -f2)
curl -s "https://api.airtable.com/v0/appEGM6qarNr9M7HN/tblkuPln7nquA3dLA/recmH8fnxcO7r3OUN" \
  -H "Authorization: Bearer $TOKEN" | python3 -c "
import json,sys
f=json.load(sys.stdin)['fields']
for k in ['업로드 기한(일)','동반 인원','반려동물 동반','드론 촬영','채널콘셉트(자기신고)','채널콘셉트']:
    print(f'{k}: {f.get(k, \"(빈 값)\")}')
"
```
기대: 신규 5필드는 `(빈 값)`, `채널콘셉트`는 운영자가 채웠으면 배열

- [ ] **Step 4: 커밋**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): 협찬 조건 필드 읽기 (Task 7)"
```

---

## Task 8: `updateCreatorProfile` — 쓰기

**Files:**
- Modify: `lib/airtable.ts` (`updateCreatorProfile`의 `updateFields`, 1570번대)

- [ ] **Step 1: import에 정규화 함수 추가**

`lib/airtable.ts` 상단의 `creatorProfileRules` import에 `normalizeUploadDeadline`을 넣는다:

```ts
import {
    validateChannelPayload,
    collectMissingForPublish,
    needsReReview,
    normalizeUploadDeadline,
} from './creatorProfileRules';
```

> 기존 import 목록이 다르면 **있는 것에 `normalizeUploadDeadline`만 더한다.** 통째로 갈아치우지 말 것.

- [ ] **Step 2: 검증 *전에* 정규화한다**

`updateCreatorProfile` 본문 맨 위, `const isPublic = payload.isPublic === true;` 줄 **아래**에 넣는다:

```ts
        // CHANGED: 2026-08-12 — 정규화를 검증보다 먼저 한다.
        // 표준과 같은 값(14)이 들어왔을 때 400을 던지면, 사용자는 "표준을 골랐는데 저장이 안 된다"를 겪는다.
        // 형태만 눕히고(14→null) 판정은 그 뒤에 하면 표준 선택이 항상 통과한다.
        const uploadDeadlineDays = normalizeUploadDeadline(payload.uploadDeadlineDays);
        const normalized: CreatorProfileUpdate = { ...payload, uploadDeadlineDays };
```

그리고 **② 채널·콘텐츠 정합 검증**의 인자를 `normalized`로 바꾼다:

```ts
        const channelViolation = validateChannelPayload(normalized);
```

**③ 공개 게이팅**도 마찬가지다:

```ts
            const missing = collectMissingForPublish(normalized, hasImage, hasPremium);
```

> ⚠️ **`needsReReview`에 넘기는 인자는 바꾸지 마라.** 신규 5필드는 재검토 판정 대상이 아니므로(스펙 E7) 정규화 여부와 무관하다. 기존 `payload` 그대로 둔다.

- [ ] **Step 3: 쓰기 필드 추가**

`'크리에이터 이메일': payload.creatorEmail || '',` 줄 **아래**에 추가:

```ts
            // CHANGED: 2026-08-12 협찬 조건 표준화
            // 빈 값이 곧 "표준 적용 중"(스펙 §9) — 14를 저장하면 나중에 표준을 15로 바꿔도 이 사람만 14로 남는다
            '업로드 기한(일)': uploadDeadlineDays,
            '동반 인원': payload.companions > 0 ? payload.companions : null,
            '반려동물 동반': payload.petAllowed === true,
            '드론 촬영': payload.droneUsed === true,
            '채널콘셉트(자기신고)': payload.channelConcepts || [],
```

> ⚠️ **`'채널콘셉트'`(운영자 필드)에는 절대 쓰지 마라.** 353명 중 170명의 영업 분류가 지워진다. `updateFields`에 그 키가 없는지 눈으로 확인할 것.

- [ ] **Step 4: 운영자 필드를 안 건드리는지 확인**

```bash
grep -n "'채널콘셉트'" lib/airtable.ts
```
기대: `getCreatorProfile`의 **읽기 1줄만** 나온다. `updateFields` 쪽에 나오면 즉시 지운다.

- [ ] **Step 5: 기한 14가 400을 안 내는지 확인**

```bash
npx tsx -e "
import { normalizeUploadDeadline, validateChannelPayload } from './lib/creatorProfileRules';
const d = normalizeUploadDeadline(14);
console.log('14 정규화 →', d, '| 정규화 후 검증 →', d === null ? 'null이라 통과' : '재확인 필요');
"
```
기대: `14 정규화 → null | 정규화 후 검증 → null이라 통과`

- [ ] **Step 6: 타입검사**

```bash
npx tsc --noEmit
```
기대: 오류 0

- [ ] **Step 7: 커밋**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): 협찬 조건 필드 쓰기 + 표준값 클리어 (Task 8)"
```

---

## Task 9: API 라우트 파싱

**Files:**
- Modify: `app/api/creator/profile/route.ts:87-106` (`payload` 객체)

> ⚠️ **이 Task를 빠뜨리면 폼이 아무리 보내도 서버 도달 전에 버려진다.** v2 §38.6에서 정확히 이 레이어를 놓쳤다.

- [ ] **Step 1: 방어적 파싱에 5필드 추가**

`creatorEmail: typeof body.creatorEmail === 'string' ? body.creatorEmail : '',` 줄 **아래**에 추가:

```ts
            // CHANGED: 2026-08-12 협찬 조건 표준화. 검증은 updateCreatorProfile이 한다
            uploadDeadlineDays: typeof body.uploadDeadlineDays === 'number' ? body.uploadDeadlineDays : null,
            companions: typeof body.companions === 'number' ? body.companions : 0,
            petAllowed: body.petAllowed === true,
            droneUsed: body.droneUsed === true,
            channelConcepts: Array.isArray(body.channelConcepts) ? body.channelConcepts : [],
```

- [ ] **Step 2: 타입검사 + 빌드**

```bash
npx tsc --noEmit && npm run build
```
기대: 둘 다 성공

- [ ] **Step 3: 서버 검증이 실제로 막는지 확인**

dev 서버를 띄우고 테스트 계정으로 로그인한 뒤, 브라우저 콘솔에서 실행한다:

```js
await fetch('/api/creator/profile', {
  method: 'PATCH', headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    representativeLink: 'https://a', minSponsorAmount: 350000,
    visitRegions: ['충청남도'], visitDays: ['월'], acceptSiteTypes: ['오토캠핑'],
    baseRegion: '충청남도', wonjeongRegions: [], isPublic: false,
    channelTypes: ['인스타'], representativeChannel: '인스타',
    channels: { '인스타': { url: 'https://i', follower: 0, engagement: 0, blogIndex: '', strength: '' } },
    representativeLink2: '', representativeLink3: '', contentFormats: [],
    contentStandard: '', creatorEmail: 'a@b.com',
    uploadDeadlineDays: 7, companions: 99, petAllowed: false, droneUsed: false,
    channelConcepts: ['서핑']
  })
}).then(async r => r.status + ' ' + JSON.stringify(await r.json()))
```
기대: `400` + `detail`이 `UPLOAD_DEADLINE_INVALID` (검증 순서상 기한이 먼저 걸린다)

- [ ] **Step 4: 커밋**

```bash
git add app/api/creator/profile/route.ts
git commit -m "feat(jimyeong): API 라우트 협찬 조건 파싱 (Task 9)"
```

---

# Phase C — UI

## Task 10: `ContentFormatFields` — 표준 배너 + 기한 3택

**Files:**
- Modify: `components/ContentFormatFields.tsx`

- [ ] **Step 1: Props와 import 확장**

파일 상단을 아래로 교체한다:

```tsx
// ContentFormatFields.tsx - 제작 콘텐츠 형식(보유 채널로 필터) + 제작 기준 (지명형 1a-v2 §3.3)
// CHANGED: 2026-08-12 — 산출물·업로드 기한 표준 배너 + 기한 예외 선택
'use client';

import { CONTENT_FORMATS, UPLOAD_DEADLINE_DEFAULT_DAYS, UPLOAD_DEADLINE_OPTIONS } from '@/lib/constants';
import { isFormatAvailable } from '@/lib/creatorProfileRules';
import { buildDeliverableSummary } from '@/lib/sponsorshipTerms';

interface ContentFormatFieldsProps {
    channelTypes: string[];
    contentFormats: string[];
    contentStandard: string;
    uploadDeadlineDays: number | null;
    onChange: (patch: {
        contentFormats?: string[];
        contentStandard?: string;
        uploadDeadlineDays?: number | null;
    }) => void;
}

export default function ContentFormatFields({
    channelTypes, contentFormats, contentStandard, uploadDeadlineDays, onChange,
}: ContentFormatFieldsProps) {
```

- [ ] **Step 2: 표준 배너 + 기한 선택 JSX 추가**

`콘텐츠 제작 기준` textarea를 감싼 `<div>` **위**에 넣는다:

```tsx
            {/* CHANGED: 2026-08-12 — 캠지기가 무엇을 언제 받는지 이 자리에서 확정된다.
                문구는 buildDeliverableSummary 한 곳에서만 만든다(캠지기 카드·제안서와 동일 문장). */}
            <div className="bg-brand-bg border border-brand/30 rounded-lg p-4">
                <p className="text-xs text-ink2 mb-1">캠지기가 받게 되는 것</p>
                <p className="text-sm font-bold text-ink leading-relaxed">
                    {buildDeliverableSummary(contentFormats, uploadDeadlineDays)}
                </p>
            </div>

            <div>
                <label className="block text-sm font-medium text-ink mb-1">업로드 기한</label>
                <p className="text-xs text-ink3 mb-2">
                    더 여유가 필요하시면 늘리실 수 있어요. 늘린 기한은 캠지기에게도 그대로 보입니다.
                </p>
                <div className="flex flex-wrap gap-2">
                    <button
                        type="button"
                        onClick={() => onChange({ uploadDeadlineDays: null })}
                        aria-pressed={uploadDeadlineDays === null}
                        className={`px-4 h-10 rounded-full border text-sm transition-colors ${
                            uploadDeadlineDays === null
                                ? 'bg-brand text-black border-brand font-bold'
                                : 'bg-card text-ink2 border-line hover:border-strong'
                        }`}
                    >
                        {UPLOAD_DEADLINE_DEFAULT_DAYS}일 (표준)
                    </button>
                    {UPLOAD_DEADLINE_OPTIONS.map((days) => (
                        <button
                            key={days}
                            type="button"
                            onClick={() => onChange({ uploadDeadlineDays: days })}
                            aria-pressed={uploadDeadlineDays === days}
                            className={`px-4 h-10 rounded-full border text-sm transition-colors ${
                                uploadDeadlineDays === days
                                    ? 'bg-brand text-black border-brand font-bold'
                                    : 'bg-card text-ink2 border-line hover:border-strong'
                            }`}
                        >
                            {days}일
                        </button>
                    ))}
                </div>
            </div>
```

- [ ] **Step 3: 타입검사**

```bash
npx tsc --noEmit
```
기대: `PortfolioEditForm.tsx`에서 `uploadDeadlineDays` prop 누락 오류 **1건**. Task 13에서 해소되므로 여기서는 정상이다.

- [ ] **Step 4: 커밋**

```bash
git add components/ContentFormatFields.tsx
git commit -m "feat(jimyeong): 산출물 표준 배너 + 업로드 기한 선택 (Task 10)"
```

---

## Task 11: `AcceptanceConditionFields` — 현장 조건 3종

**Files:**
- Modify: `components/AcceptanceConditionFields.tsx`

- [ ] **Step 1: Props 확장**

`ConditionPatch` 타입과 `AcceptanceConditionFieldsProps`에 3필드를 더한다:

```tsx
type ConditionPatch = Partial<{
    baseRegion: string;
    visitRegions: string[];
    wonjeongRegions: string[];
    visitDays: string[];
    acceptSiteTypes: string[];
    minSponsorAmount: number;
    // CHANGED: 2026-08-12 현장 조건
    companions: number;
    petAllowed: boolean;
    droneUsed: boolean;
}>;

interface AcceptanceConditionFieldsProps {
    baseRegion: string;
    baseRegionPrefill: string;
    visitRegions: string[];
    wonjeongRegions: string[];
    visitDays: string[];
    acceptSiteTypes: string[];
    minSponsorAmount: number;
    companions: number;
    petAllowed: boolean;
    droneUsed: boolean;
    onChange: (patch: ConditionPatch) => void;
}
```

구조 분해에도 추가한다:

```tsx
export default function AcceptanceConditionFields({
    baseRegion,
    baseRegionPrefill,
    visitRegions,
    wonjeongRegions,
    visitDays,
    acceptSiteTypes,
    minSponsorAmount,
    companions,
    petAllowed,
    droneUsed,
    onChange,
}: AcceptanceConditionFieldsProps) {
```

- [ ] **Step 2: import에 상수 추가**

```tsx
import {
    VISIT_REGIONS,
    VISIT_DAYS,
    SPONSOR_SITE_TYPES,
    WONJEONG_SURCHARGE,
    COMPANION_MIN,
    COMPANION_MAX,
    getWonjeongCandidates,
} from '@/lib/constants';
```

- [ ] **Step 3: 현장 조건 JSX 추가**

`{/* 6. 협찬 금액 */}` 블록 **위**에 넣는다:

```tsx
            {/* CHANGED: 2026-08-12 — 현장 조건(스펙 §3).
                반려동물·드론은 표준이 "없음"이라 켰을 때만 캠지기가 사전 확인하면 된다.
                동반 인원만 표준으로 못 덮는다 — 사이트 정원은 물리적 제약이다(스펙 E3). */}
            <div>
                <label className="block text-sm font-medium text-ink mb-1">
                    동반 인원 <span className="text-red-500">*</span>
                </label>
                <p className="text-xs text-ink3 mb-2">
                    본인 포함 몇 분이 방문하시나요? 캠지기가 사이트를 이 인원에 맞춰 잡아둡니다.
                </p>
                <div className="relative">
                    <input
                        type="number"
                        inputMode="numeric"
                        min={COMPANION_MIN}
                        max={COMPANION_MAX}
                        value={companions || ''}
                        onChange={(event) => onChange({ companions: Number(event.target.value) || 0 })}
                        placeholder="예: 2"
                        className="w-full h-12 px-4 pr-10 bg-card border border-line rounded-lg text-ink text-sm focus:border-brand focus:outline-none transition-colors placeholder:text-ink3"
                    />
                    <span className="absolute right-4 top-1/2 -translate-y-1/2 text-ink3 text-sm">명</span>
                </div>
            </div>

            <div className="flex flex-col gap-2">
                <button
                    type="button"
                    onClick={() => onChange({ petAllowed: !petAllowed })}
                    aria-pressed={petAllowed}
                    className={`w-full h-12 px-4 rounded-lg border text-sm text-left transition-colors ${
                        petAllowed
                            ? 'bg-brand-bg text-brand-strong border-brand/30 font-medium'
                            : 'bg-card text-ink2 border-line hover:border-strong'
                    }`}
                >
                    {petAllowed ? '✓ ' : ''}반려동물과 함께 방문해요
                </button>
                <button
                    type="button"
                    onClick={() => onChange({ droneUsed: !droneUsed })}
                    aria-pressed={droneUsed}
                    className={`w-full h-12 px-4 rounded-lg border text-sm text-left transition-colors ${
                        droneUsed
                            ? 'bg-brand-bg text-brand-strong border-brand/30 font-medium'
                            : 'bg-card text-ink2 border-line hover:border-strong'
                    }`}
                >
                    {droneUsed ? '✓ ' : ''}드론으로 촬영해요
                </button>
                <p className="text-xs text-ink3">
                    두 가지는 캠핑장마다 가능 여부가 달라요. 체크하시면 캠지기가 미리 확인합니다.
                </p>
            </div>

            {/* CHANGED: 2026-08-12 — 캠지기에게 어떻게 보이는지 그 자리에서 확인시킨다.
                문구는 buildVisitConditionSummary 한 곳에서만 만든다(캠지기 카드·제안서와 동일 문장). */}
            {visitConditionSummary && (
                <div className="bg-subtle border border-line rounded-lg p-3">
                    <p className="text-xs text-ink3 mb-1">캠지기에게 이렇게 보여요</p>
                    <p className="text-sm text-ink font-medium">{visitConditionSummary}</p>
                </div>
            )}
```

- [ ] **Step 4: 요약 문구 계산부 추가**

`const surcharge = WONJEONG_SURCHARGE.toLocaleString();` 줄 **아래**에 넣는다:

```tsx
    const visitConditionSummary = buildVisitConditionSummary(companions, petAllowed, droneUsed);
```

import도 추가한다:

```tsx
import { buildVisitConditionSummary } from '@/lib/sponsorshipTerms';
```

- [ ] **Step 5: 타입검사**

```bash
npx tsc --noEmit
```
기대: `PortfolioEditForm.tsx`에서 prop 누락 오류. Task 13에서 해소된다.

- [ ] **Step 6: 커밋**

```bash
git add components/AcceptanceConditionFields.tsx
git commit -m "feat(jimyeong): 현장 조건 입력 3종 (Task 11)"
```

---

## Task 12: `ChannelConceptField` 신설

**Files:**
- Create: `components/ChannelConceptField.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// ChannelConceptField.tsx - 채널콘셉트 자기신고 (2026-08-12 스펙 E6)
// ⚠️ 운영자 관리 필드 `채널콘셉트`(353명 중 170명 보유)는 건드리지 않는다.
//    크리에이터가 고른 값은 별도 `채널콘셉트(자기신고)`에만 저장된다.
'use client';

import { CHANNEL_CONCEPTS } from '@/lib/constants';

interface ChannelConceptFieldProps {
    concepts: string[];          // 자기신고 — 크리에이터가 고른 것
    fallbackConcepts: string[];  // 운영자 값. 자기신고가 비었을 때만 쓰인다
    onChange: (concepts: string[]) => void;
}

export default function ChannelConceptField({ concepts, fallbackConcepts, onChange }: ChannelConceptFieldProps) {
    // 자기신고가 하나라도 있으면 그게 곧 표시값이다. 없을 때만 운영자 값이 보인다.
    const usingFallback = concepts.length === 0 && fallbackConcepts.length > 0;

    function toggleConcept(concept: string) {
        const next = concepts.includes(concept)
            ? concepts.filter((c) => c !== concept)
            : [...concepts, concept];
        onChange(next);
    }

    return (
        <div className="flex flex-col gap-2">
            <label className="block text-sm font-medium text-ink">채널 콘셉트</label>
            <p className="text-xs text-ink3">
                캠지기가 우리 캠핑장에 맞는 분인지 볼 때 씁니다. 여러 개 고르셔도 돼요.
            </p>

            {usingFallback && (
                <div className="bg-subtle border border-line rounded-lg p-3">
                    <p className="text-xs text-ink2 leading-relaxed">
                        지금은 캠핏 운영팀이 분류한 <strong className="text-ink">{fallbackConcepts.join(' · ')}</strong>로
                        보이고 있어요. 직접 고르시면 그 값으로 바뀝니다.
                    </p>
                </div>
            )}

            <div className="flex flex-wrap gap-2">
                {CHANNEL_CONCEPTS.map((concept) => {
                    const selected = concepts.includes(concept);
                    return (
                        <button
                            key={concept}
                            type="button"
                            onClick={() => toggleConcept(concept)}
                            aria-pressed={selected}
                            className={`px-4 h-10 rounded-full border text-sm transition-colors ${
                                selected
                                    ? 'bg-brand-bg text-brand-strong border-brand/30 font-medium'
                                    : 'bg-card text-ink2 border-line hover:border-strong'
                            }`}
                        >
                            {concept}
                        </button>
                    );
                })}
            </div>
        </div>
    );
}
```

- [ ] **Step 2: 타입검사 + 린트**

```bash
npx tsc --noEmit; npx eslint components/ChannelConceptField.tsx
```
기대: eslint 0 error (tsc는 Task 13 전까지 prop 누락 오류가 남아 있다)

- [ ] **Step 3: 커밋**

```bash
git add components/ChannelConceptField.tsx
git commit -m "feat(jimyeong): 채널 콘셉트 자기신고 입력 (Task 12)"
```

---

## Task 13: `PortfolioEditForm` 배선

**Files:**
- Modify: `components/PortfolioEditForm.tsx`

이 Task가 끝나면 `tsc`가 다시 통과해야 한다.

- [ ] **Step 1: import 추가**

```tsx
import ChannelConceptField from './ChannelConceptField';
```

- [ ] **Step 2: `buildPayload`에 5필드 추가**

`creatorEmail: profile.creatorEmail,` 줄 **아래**에 넣는다:

```tsx
        // CHANGED: 2026-08-12 협찬 조건 표준화
        uploadDeadlineDays: profile.uploadDeadlineDays,
        companions: profile.companions,
        petAllowed: profile.petAllowed,
        droneUsed: profile.droneUsed,
        channelConcepts: profile.channelConcepts,
```

> `channelConceptsFallback`은 **넣지 않는다.** `CreatorProfileUpdate`에 없는 필드다(운영자 필드는 크리에이터가 못 바꾼다).

- [ ] **Step 3: 완성도 계산에 폴백 인자 전달**

`const { percent, nextHint } = computeCompletion(...)` 줄을 아래로 교체한다:

```tsx
    const { percent, nextHint } = computeCompletion(
        payloadForCheck,
        profile.hasProfileImage,
        hasPremium,
        profile.channelConceptsFallback.length > 0
    );
```

- [ ] **Step 4: `ContentFormatFields`에 prop 전달**

```tsx
            <ContentFormatFields
                channelTypes={profile.channelTypes}
                contentFormats={profile.contentFormats}
                contentStandard={profile.contentStandard}
                uploadDeadlineDays={profile.uploadDeadlineDays}
                onChange={patchProfile}
            />
```

- [ ] **Step 5: `ChannelConceptField`를 "내 채널" 섹션에 추가**

`<ChannelDetailTabs ... />` **아래**에 넣는다:

```tsx
            <ChannelConceptField
                concepts={profile.channelConcepts}
                fallbackConcepts={profile.channelConceptsFallback}
                onChange={(channelConcepts) => patchProfile({ channelConcepts })}
            />
```

- [ ] **Step 6: `AcceptanceConditionFields`에 prop 전달**

```tsx
            <AcceptanceConditionFields
                baseRegion={profile.baseRegion}
                baseRegionPrefill={settlement?.baseRegionPrefill || ''}
                visitRegions={profile.visitRegions}
                wonjeongRegions={profile.wonjeongRegions}
                visitDays={profile.visitDays}
                acceptSiteTypes={profile.acceptSiteTypes}
                minSponsorAmount={profile.minSponsorAmount}
                companions={profile.companions}
                petAllowed={profile.petAllowed}
                droneUsed={profile.droneUsed}
                onChange={patchProfile}
            />
```

- [ ] **Step 7: 타입검사 + 린트**

```bash
npx tsc --noEmit && npx eslint components app lib types
```
기대: tsc 오류 **0** / eslint는 기존 6건 외 신규 0

- [ ] **Step 8: 커밋**

```bash
git add components/PortfolioEditForm.tsx
git commit -m "feat(jimyeong): 협찬 조건 폼 배선 (Task 13)"
```

---

# Phase D — 검증

## Task 14: 전체 정적 검증

- [ ] **Step 1: 4단 검증 전부**

```bash
npx tsx tools/jimyeong/verify-rules.ts && npx tsc --noEmit && npm run build && npx eslint components app lib types
```
기대: `62 passed, 0 failed` / tsc 0 / build 성공 / eslint 신규 0

- [ ] **Step 2: 운영자 필드 보호 확인**

```bash
grep -n "'채널콘셉트'" lib/airtable.ts
```
기대: **읽기 1줄만**. `updateFields` 쪽에 있으면 즉시 제거하고 커밋한다.

- [ ] **Step 3: 표준값이 하드코딩되지 않았는지 확인**

```bash
grep -rn "14일\|체크아웃 후" components/ lib/ --include="*.tsx" --include="*.ts" | grep -v sponsorshipTerms.ts
```
기대: 출력 없음. 문구는 `buildDeliverableSummary` 한 곳에서만 만들어야 한다.

- [ ] **Step 4: 커밋** (수정이 있었다면)

---

## Task 15: 브라우저 종단 검증

**선행:** Task 0 완료

> ⚠️ **클릭 전 `scroll_to` 필수.** 오프스크린 요소는 클릭이 에러 없이 조용히 무시된다(v2 §39.6에서 데인 곳).

- [ ] **Step 1: dev 서버 + 테스트 계정 로그인**

테스트 계정 `[TEST]지명형확인_삭제예정`(`recmH8fnxcO7r3OUN`) / 연락처 뒤 4자리 `7777`

- [ ] **Step 2: 확인 항목**

| # | 확인 | 기대 |
|---|---|---|
| 1 | 형식 미선택 상태의 배너 | "체크아웃 후 14일 안에 업로드" |
| 2 | 형식 2개 선택 | "○○ 1편 · ○○ 1편 — 체크아웃 후 14일 안에 업로드" |
| 3 | 기한 21일 선택 | 배너가 "21일"로 즉시 바뀜 |
| 4 | 기한 21일 저장 | Airtable `업로드 기한(일)` = **21** |
| 5 | 기한을 표준으로 되돌리고 저장 | Airtable `업로드 기한(일)`이 **빈 값** (14가 아님) |
| 6 | 동반 인원 미입력 상태로 공개 시도 | 누락 칩에 **`동반 인원`** 표시, 버튼 비활성 |
| 7 | 동반 인원 2 입력 | 누락 칩에서 사라짐 |
| 8 | 반려동물·드론 체크 후 저장 | Airtable 두 필드 **true** |
| 9 | 채널콘셉트 — 자기신고 비었고 운영자 값 있음 | "운영팀이 분류한 ○○로 보이고 있어요" 안내 표시 |
| 10 | 채널콘셉트 2개 선택 후 저장 | `채널콘셉트(자기신고)`에 저장, **`채널콘셉트`는 변화 없음** |
| 11 | 승인 상태에서 콘셉트만 수정 | `지표 재검토 필요` **안 섬** (스펙 E7) |
| 12 | 375px | 칩 줄바꿈 정상, 가로 스크롤 없음 |

- [ ] **Step 3: 서버 우회 시도**

Task 9 Step 3의 fetch를 다시 실행한다.
기대: `400` + `detail: 'UPLOAD_DEADLINE_INVALID'`

- [ ] **Step 4: 🛑 사용자 리뷰**

배포 전 사용자가 직접 확인한다.

---

## Task 16: 캠지기 회신 + 핸드오버

**Files:**
- Create: `docs/회신-캠지기-2026-08-12.md`
- Modify: `handoff_2026-08-11.md`

- [ ] **Step 1: 캠지기 회신 문서 작성**

스펙 §7의 5건을 그대로 옮기고 각 항목에 근거(필드명·실측 수치)를 붙인다. 특히 **1번(빈 값이면 표준 렌더)** 은 이게 없으면 B안 전체가 무너진다는 점을 명시한다.

- [ ] **Step 2: 핸드오버에 섹션 추가**

`handoff_2026-08-11.md`에 §40을 추가한다. 기존 형식(현재 단계 / 작업 내역 / 주요 결정사항 / 실수·배운 점 / 검증 / 다음 할 일 / 참고)을 따른다.

- [ ] **Step 3: 커밋**

```bash
git add docs/회신-캠지기-2026-08-12.md
git commit -m "docs(jimyeong): 협찬 조건 표준화 캠지기 회신 (Task 16)"
```

> `handoff_2026-08-11.md`는 git 미추적 로컬 문서다. **`git add` 하지 않는다.**

---

# 배포 전 남은 게이트

| # | 항목 | 담당 |
|---|---|---|
| 1 | 🔴 **캠지기 화면이 "빈 값 → 표준 렌더"를 구현** — 없으면 B안 전체가 무효 | 캠지기측 |
| 2 | Airtable 신규 필드 5개 생성 (Task 0) | 캠지기측 |
| 3 | `채널콘셉트(자기신고)` 우선 · 없으면 `채널콘셉트` 폴백 렌더 | 캠지기측 |
| 4 | `조건 스냅샷` 기록 주체 확정 (1b/어드민) | 사용자 → 캠지기 |
| 5 | 테스트 레코드 `recmH8fnxcO7r3OUN` 삭제 | 사용자 |
