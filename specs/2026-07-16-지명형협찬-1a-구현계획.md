# 지명형 협찬 1a (포트폴리오+조건+정산) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크리에이터가 `/dashboard/portfolio` 한 페이지에서 포트폴리오(이미지·대표콘텐츠) + 협찬 수락 조건(지역·요일·사이트유형·최소단가) + 정산 온보딩을 설정하고, 전부 채우면 `프로필 공개`(옵트인)를 켜는 화면을 만든다.

**Architecture:** 크리에이터 명단(`tblkuPln7nquA3dLA`) 레코드를 JWT `creatorId`로만 읽고/쓴다(본인 것만, IDOR 방어). 이미지는 Airtable `uploadAttachment` REST 엔드포인트로 외부 호스트 없이 첨부 필드에 직접 올린다. 정산정보는 기존 프리미엄 유저 테이블(`tblDOC7jcmeuQzNJY`)에서 마스킹 조회. 신규 API는 `app/api/creator/*`, 신규 UI는 `components/Portfolio*` + `app/dashboard/portfolio`.

**Tech Stack:** Next.js 16(App Router) · React 19(함수형) · TypeScript(strict) · Tailwind v4(시맨틱 토큰) · `jose`(JWT) · `airtable` SDK + `uploadAttachment` raw fetch.

> **테스트 정책:** 이 repo엔 유닛 테스트 러너가 없다(scripts: `dev`/`build`/`start`/`lint`). 각 태스크 검증 = `npm run lint` → 필요 시 `npm run build` → dev 서버 브라우저 프리뷰 → Airtable MCP로 레코드 반영 확인. 커밋은 태스크 단위.

> **참조 패턴(그대로 따를 것):**
> - JWT 가드 + PATCH: [app/api/notifications/toggle/route.ts](app/api/notifications/toggle/route.ts) (`cookies()` + `jwtVerify` + `payload.creatorId || payload.id`)
> - Airtable 접근: [lib/airtable.ts](lib/airtable.ts) `getBase()`/`creatorTable()`/`escapeAirtableValue()`/`updateCreatorNotification()`
> - 폼 UI: [components/PremiumRegisterForm.tsx](components/PremiumRegisterForm.tsx) `SectionTitle`/`FormInput`
> - 디자인 토큰: `bg-page/bg-card/border-line/text-ink/text-ink3/bg-brand/text-brand-strong/bg-brand-bg` (하드코딩 hex 금지)

---

## ⚠️ 실측 반영 델타 (2026-07-16 검증 + 옵트인 모델 확정) — 아래 코드 스니펫보다 **우선**

이 계획의 최초 초안은 (a) 사이트 유형 필드가 신설 필요라고 봤고 (b) 옵트인을 `프로필 공개` 단일 플래그로 설계했다. **Airtable 실측 + 사용자 결정으로 아래가 확정**되었으니, 본문 코드 스니펫과 충돌 시 **이 블록을 따른다.**

### D1. 사이트 유형 필드명 = `수용 사이트 종류`(fldWmvzbNngsSoNrZ)
본문의 `record.get('수용 사이트 종류')` / `'수용 사이트 종류': ...`는 이미 실제명으로 치환됨. 옵션 8종은 초안 그대로 유효.

### D2. 옵트인 = 2단 플래그(Airbnb식) — `자동수락 활성`(fld15wHeuJuwUOf8R) 추가
`프로필 공개`(가시성) 외에 **`자동수락 활성`(인스턴트북 동의)** 를 함께 다룬다. `isPublic`을 스레딩하는 모든 지점에 `autoAcceptActive`를 나란히 추가:

- **types (Task 2)** — `CreatorProfile`·`CreatorProfileUpdate`에 `autoAcceptActive: boolean;` 추가.
- **getCreatorProfile (Task 3)** — `autoAcceptActive: record.get('자동수락 활성') === true,` 추가.
- **updateCreatorProfile (Task 4)** — 저장 필드에 `'자동수락 활성': payload.autoAcceptActive` 추가. **서버 규칙**: ① `autoAcceptActive === true`인데 최종 `isPublic !== true`면 400(`AUTO_ACCEPT_REQUIRES_PUBLIC`). ② `isPublic === false`로 내리는 저장은 `자동수락 활성`도 **강제 false**로 동기화(payload 값 무시). ③ `프로필 공개` 게이팅(§5 필수항목 서버 재검증)은 초안 그대로.
- **API PATCH (Task 5)** — 파싱에 `autoAcceptActive: body.autoAcceptActive === true` 추가.
- **PublishToggle (Task 9)** — **2단 토글**로 재설계: ① `프로필 공개` 주 토글(§5 미완 시 disabled + 빠진 항목 안내). ② `자동수락 활성` 하위 토글 — `isPublic===false`면 disabled, ON 시도 시 **자동수락 동의 모달** 띄우고 동의해야 true. `프로필 공개`를 OFF로 내리면 `자동수락 활성` UI도 즉시 OFF 반영.
- **PortfolioEditForm (Task 10+)** — 상태·save 페이로드에 `autoAcceptActive` 스레딩. `프로필 공개` OFF 시 `자동수락 활성`=false 동기화(낙관적 UI).

### D3. 정산 `은행` 필드는 singleSelect + 동반 필드 (SettlementConfirmCard)
유저 테이블 `은행`(fldsMhrz0hoaiAZj0)은 singleSelect(10지선다). 값이 `기타(직접입력)`이면 동반 `기타 은행 (직접 입력)`(fldBf9NmebWhWa453)을 읽어 표시. 계좌번호는 뒤 4자리만, `주민등록번호`(fldPbBfn1W26IQI6v)·전체 계좌번호는 절대 비노출.

### D4. (P2 미결) `조건 부합 여부` function 검증 배포 확인
지명 제안 테이블에 A′ 검증 필드(`조건 부합 여부` fldoxDdZ3UBCTdRdm, `조건 스냅샷` fldh450q7YxyrXJ06, `선입금 일시` fld5q1gjNeizC3diQ)는 실측 존재. 단 이를 **채우는 서버 로직(camfit-premium)** 배포 여부는 1a와 무관(포털 밖)하나 A′ 전체 흐름 전제 — camfit-premium과 별도 확인.

### D5. 원정 인센티브(유류비) 모델 추가 — 신설 필드 2개 + 상수 + 2단 지역 플로우 (스펙 §6.4)
경상/전라 매칭 갭을 원정으로 메우는 지역 3상태(기본가/원정+유류비/불가). `방문 가능 지역`·`수용 사이트 종류`를 스레딩하는 지점에 아래를 추가:

- **신설 Airtable 필드 2개(P1'):** `기준 지역`(singleSelect 9) · `원정 가능 지역`(multipleSelects 9). 옵션값은 `방문 가능 지역`과 동일 9개.
- **constants (Task 1):** 추가
  - `BASE_REGIONS` = 9권역(= 기준 지역/원정 가능 지역 옵션, `VISIT_REGIONS`와 동일값 재사용 가능)
  - `WONJEONG_SURCHARGE = 100000` (유류비, 플랫폼 고정 — 크리에이터 편집 불가)
  - `WONJEONG_MAP: Record<string, string[]>` (기준 지역 → 원정 후보. 스펙 §6.4 표. 좌우 대칭. camfit-premium과 동일값 공유)
- **types (Task 2):** `CreatorProfile`·`CreatorProfileUpdate`에 `baseRegion: string`(기준 지역) · `wonjeongRegions: string[]`(원정 가능 지역) 추가. `SettlementSummary`(또는 profile)에 `baseRegionPrefill: string`(주소서 파싱한 기준 지역 후보).
- **getCreatorProfile (Task 3):** `baseRegion: (record.get('기준 지역') as string) || ''` · `wonjeongRegions: (record.get('원정 가능 지역') as string[]) || []` 읽기. premiumId 정산 조회 시 `주소`에서 시/도 파싱 → `baseRegionPrefill`.
- **updateCreatorProfile (Task 4):** 저장에 `'기준 지역': payload.baseRegion` · `'원정 가능 지역': payload.wonjeongRegions` 추가. **서버 검증(위반 시 400)**: ① `wonjeongRegions ⊆ WONJEONG_MAP[baseRegion]` ② `wonjeongRegions ∩ visitRegions = ∅`(상호배타).
- **API PATCH (Task 5):** 파싱에 `baseRegion: typeof body.baseRegion === 'string' ? body.baseRegion : ''` · `wonjeongRegions: Array.isArray(body.wonjeongRegions) ? body.wonjeongRegions : []`.
- **AcceptanceConditionFields (Task 8):** 지역 입력을 **2단**으로: (1) `기준 지역` 확인(baseRegionPrefill 기본값) → (2) `방문 가능 지역` 멀티선택(기본가) → (3) 원정 제안: `WONJEONG_MAP[baseRegion]` 중 방문가능 미포함 지역만 노출, 지역별 [예/아니오]. 예→`wonjeongRegions`에 추가 + 인라인 안내문("이 지역은 캠지기가 유류비 10만 원 얹어 제안하면, 자동수락 켜져 있을 때 자동확정"). 유류비 표기는 `WONJEONG_SURCHARGE`.

---

## 선행 조건 (사람이 먼저 — 코드 아님)

- [x] **P1. Airtable 필드 신설 — ✅ 완료(2026-07-16 실측 검증):** 크리에이터 명단(`tblkuPln7nquA3dLA`)에 `수용 사이트 종류`(fldWmvzbNngsSoNrZ, multipleSelects 8종)가 이미 존재. 옵션 8종(`글램핑` `두가족존` `방갈로` `오토캠핑` `이지캠핑` `차박` `카라반` `펜션`)이 지명 제안 `협찬 사이트 종류`(fldbnfHs4vWpM7QSH)와 **문자열 정확 일치**. 옵트인 ② 플래그 `자동수락 활성`(fld15wHeuJuwUOf8R)도 기생성 확인. ⚠️ 코드는 실제 필드명 `수용 사이트 종류`로 접근(최초 가정명 아님).
- [ ] **P1'. 🆕 원정 모델 필드 신설(착수 전 필요):** 크리에이터 명단에 `기준 지역`(singleSelect 9) · `원정 가능 지역`(multipleSelects 9). 옵션값 = `방문 가능 지역`과 동일 9개. 운영/데이터팀 작업. (스펙 §6.4 / D5)
- [ ] **P2. camfit-premium 합의:** 위 조건 필드(사이트유형·`협찬 희망 금액`·`방문 가능 지역`·`방문 가능 요일`)를 캠지기 제안 폼의 조건검증이 읽는다 — 필드명·옵션값 일치 확인. **원정**: `WONJEONG_MAP`·`WONJEONG_SURCHARGE(=100000)`·`기준 지역`·`원정 가능 지역` 두 레포 일치 → 매칭이 `필요단가(R)=협찬 희망 금액+(원정?10만:0)` 계산(§6.4).
- [ ] **P3. 확인:** `.env.local`에 `AIRTABLE_BASE_ID`(=`appEGM6qarNr9M7HN`)·`AIRTABLE_CREATOR_TABLE_ID`·`AIRTABLE_USER_TABLE_ID`·`AIRTABLE_ACCESS_TOKEN`·`NEXTAUTH_SECRET` 존재.

---

## 파일 구조 (생성/수정)

| 파일 | 책임 | 구분 |
|---|---|---|
| `lib/constants.ts` | 지역9·요일7·사이트유형8 옵션 + **`WONJEONG_MAP`·`WONJEONG_SURCHARGE`(원정)** 상수 | 수정 |
| `types/index.ts` | `CreatorProfile`·`SettlementSummary`·`CreatorProfileUpdate` 타입 | 수정 |
| `lib/airtable.ts` | `getCreatorProfile`·`getPremiumSettlement`·`updateCreatorProfile`·`uploadCreatorProfileImage` | 수정 |
| `app/api/creator/profile/route.ts` | GET(조회)·PATCH(저장+공개 게이팅) | 생성 |
| `app/api/creator/profile/image/route.ts` | POST(이미지 업로드) | 생성 |
| `components/MultiSelectChips.tsx` | 다중선택 칩(지역/요일/사이트유형 공용) | 생성 |
| `components/ProfileImageUploader.tsx` | 이미지 선택·리사이즈·업로드·프리뷰 | 생성 |
| `components/AcceptanceConditionFields.tsx` | 조건 입력 — **지역 2단(기준→방문가능→원정 제안)** + 요일·사이트유형·단가 | 생성 |
| `components/SettlementConfirmCard.tsx` | 정산정보 마스킹 확인/등록 유도 | 생성 |
| `components/PublishToggle.tsx` | 공개 토글 + 게이팅 + 자동수락 동의 모달 | 생성 |
| `components/PortfolioEditForm.tsx` | 4섹션 합성 + 로드/저장 | 생성 |
| `app/dashboard/portfolio/page.tsx` | 페이지 진입점 | 생성 |
| `app/dashboard/page.tsx` | "내 협찬 프로필" 진입 배너 추가 | 수정 |

---

## Task 0: 브랜치 세팅

**Files:** 없음 (git)

- [ ] **Step 1: origin 최신화 + origin/main 기준 새 브랜치**

```bash
cd /Users/choiwooseok/choiwooseok_ai/influencer-portal
git fetch origin
git switch -c feat/jimyeong-portfolio-1a origin/main
```

Expected: `feat/jimyeong-portfolio-1a` 브랜치 생성(origin/main 기준). *(CLAUDE.md §10 — 낡은 로컬 main/딴 feature 브랜치 위에서 시작 금지)*

- [ ] **Step 2: 스펙 문서 커밋**

```bash
git add specs/2026-07-16-지명형협찬-1a-포트폴리오-조건-정산.md specs/2026-07-16-지명형협찬-1a-구현계획.md
git commit -m "docs(jimyeong): 지명형 1a 포트폴리오+조건+정산 스펙·계획"
```

---

## Task 1: 옵션 상수 (지역·요일·사이트유형)

**Files:**
- Modify: `lib/constants.ts` (파일 끝에 추가)

- [ ] **Step 1: 상수 추가**

`lib/constants.ts` 끝에 추가 (Airtable 옵션값과 문자열 정확히 일치):

```ts
// ─────────────────────────────────────────────────────────
// 지명형 협찬 1a — 크리에이터 수락 조건 옵션 (Airtable 크리에이터 명단 옵션값과 정확히 일치)
// ─────────────────────────────────────────────────────────

/** 방문 가능 지역 (9권역) — 크리에이터 명단 `방문 가능 지역` multipleSelects */
export const VISIT_REGIONS: string[] = [
    '경기도(서울, 인천 포함)', '강원도', '충청북도', '충청남도',
    '전라북도', '전라남도', '경상북도', '경상남도', '제주도'
];

/** 방문 가능 요일 (7일) — 크리에이터 명단 `방문 가능 요일` multipleSelects */
export const VISIT_DAYS: string[] = ['월', '화', '수', '목', '금', '토', '일'];

/** 수용 사이트 종류 (8종) — 크리에이터 명단 `수용 사이트 종류`(fldWmvzbNngsSoNrZ) = 지명 제안 `협찬 사이트 종류`와 동일 옵션 */
export const SPONSOR_SITE_TYPES: string[] = [
    '글램핑', '두가족존', '방갈로', '오토캠핑', '이지캠핑', '차박', '카라반', '펜션'
];
```

- [ ] **Step 2: lint**

Run: `npm run lint`
Expected: 통과(신규 export만 추가, 에러 없음)

- [ ] **Step 3: commit**

```bash
git add lib/constants.ts
git commit -m "feat(jimyeong): 수락 조건 옵션 상수(지역·요일·사이트유형)"
```

---

## Task 2: 도메인 타입

**Files:**
- Modify: `types/index.ts` (파일 끝에 추가)

- [ ] **Step 1: 타입 추가**

`types/index.ts` 끝에 추가:

```ts
// ─────────────────────────────────────────────────────────
// 지명형 협찬 1a — 크리에이터 포트폴리오/조건/정산
// ─────────────────────────────────────────────────────────

/** 편집 화면이 읽어오는 크리에이터 프로필 전체 상태 */
export interface CreatorProfile {
    // 포트폴리오
    profileImageUrl: string;   // 첫 첨부의 (만료성) URL. 없으면 ''
    hasProfileImage: boolean;
    representativeLink: string; // 대표 콘텐츠 링크
    // 협찬 수락 조건
    minSponsorAmount: number;   // 협찬 희망 금액 (= 최소 단가). 미설정 0
    visitRegions: string[];     // 방문 가능 지역
    visitDays: string[];        // 방문 가능 요일
    acceptSiteTypes: string[];  // 수용 사이트 종류
    // 공개
    isPublic: boolean;          // 프로필 공개
    // 표시 전용(운영자 관리, 편집 불가)
    channelName: string;
    tier: TierLevel;            // 등급화 1~3
    followerRange: string;      // 팔로워 구간
}

/** 정산정보 요약(마스킹) */
export interface SettlementSummary {
    registered: boolean;   // premiumId 존재 여부
    bank: string;          // 은행 (없으면 '')
    accountLast4: string;  // 계좌번호 뒤 4자리
    accountHolder: string; // 예금주
}

/** 저장 페이로드(편집 가능 필드만) */
export interface CreatorProfileUpdate {
    representativeLink: string;
    minSponsorAmount: number;
    visitRegions: string[];
    visitDays: string[];
    acceptSiteTypes: string[];
    isPublic: boolean;
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add types/index.ts
git commit -m "feat(jimyeong): CreatorProfile/SettlementSummary/Update 타입"
```

---

## Task 3: lib — getCreatorProfile

**Files:**
- Modify: `lib/airtable.ts`

- [ ] **Step 1: import에 타입 추가**

`lib/airtable.ts` 상단 타입 import 블록(`import type { ... } from '@/types';`)에 추가:

```ts
    CreatorProfile,
    SettlementSummary,
    CreatorProfileUpdate,
```

- [ ] **Step 2: 함수 추가**

`lib/airtable.ts` 끝(파일 하단, 파트너 함수들 뒤)에 추가:

```ts
// ─────────────────────────────────────────────────────────
// 지명형 협찬 1a — 크리에이터 포트폴리오/조건/정산
// ─────────────────────────────────────────────────────────

/** Airtable 첨부 필드 원소 타입(부분) */
interface AirtableAttachment {
    id: string;
    url: string;
    filename?: string;
}

/**
 * 크리에이터 명단 레코드에서 포트폴리오+조건+공개 상태 조회.
 * creatorId = JWT의 크리에이터 명단 record id.
 */
export async function getCreatorProfile(creatorId: string): Promise<CreatorProfile | null> {
    try {
        const record = await creatorTable().find(creatorId);

        const attachments = (record.get('프로필 이미지') as AirtableAttachment[] | undefined) || [];
        const ratingValue = record.get('등급화');
        const tier = (ratingValue ? String(ratingValue) : '1') as TierLevel;

        return {
            profileImageUrl: attachments.length > 0 ? attachments[0].url : '',
            hasProfileImage: attachments.length > 0,
            representativeLink: (record.get('대표 콘텐츠 링크') as string) || '',
            minSponsorAmount: (record.get('협찬 희망 금액') as number) || 0,
            visitRegions: (record.get('방문 가능 지역') as string[]) || [],
            visitDays: (record.get('방문 가능 요일') as string[]) || [],
            acceptSiteTypes: (record.get('수용 사이트 종류') as string[]) || [],
            isPublic: record.get('프로필 공개') === true,
            channelName: (record.get('크리에이터 채널명') as string) || '',
            tier,
            followerRange: (record.get('팔로워 구간') as string) || ''
        };
    } catch (error) {
        console.error('Get creator profile error:', error);
        return null;
    }
}
```

- [ ] **Step 3: lint** — `npm run lint` → 통과

- [ ] **Step 4: commit**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): getCreatorProfile — 포트폴리오/조건 조회"
```

---

## Task 4: lib — getPremiumSettlement (마스킹)

**Files:**
- Modify: `lib/airtable.ts`

- [ ] **Step 1: 함수 추가** (Task 3 함수 아래)

```ts
/**
 * 크리에이터의 기존 프리미엄 정산정보 요약(마스킹) 조회.
 * 크리에이터 명단의 `프리미엄 협찬 신청 인플루언서` 링크로 유저 테이블 레코드를 찾는다.
 * 계좌번호는 뒤 4자리만 반환(전체·주민번호 노출 금지).
 */
export async function getPremiumSettlement(creatorId: string): Promise<SettlementSummary> {
    try {
        const creatorRecord = await creatorTable().find(creatorId);
        const premiumLinks = creatorRecord.get('프리미엄 협찬 신청 인플루언서') as string[] | undefined;
        const premiumId = Array.isArray(premiumLinks) && premiumLinks.length > 0 ? premiumLinks[0] : null;

        if (!premiumId) {
            return { registered: false, bank: '', accountLast4: '', accountHolder: '' };
        }

        const userRecord = await userTable().find(premiumId);
        const accountNumber = ((userRecord.get('계좌번호') as string) || '').replace(/[^0-9]/g, '');

        return {
            registered: true,
            bank: (userRecord.get('은행') as string) || '',
            accountLast4: accountNumber.slice(-4),
            accountHolder: (userRecord.get('예금주') as string) || ''
        };
    } catch (error) {
        console.error('Get premium settlement error:', error);
        // 조회 실패해도 화면은 떠야 하므로 미등록으로 폴백
        return { registered: false, bank: '', accountLast4: '', accountHolder: '' };
    }
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): getPremiumSettlement — 정산정보 마스킹 조회"
```

---

## Task 5: lib — updateCreatorProfile (+ 서버 공개 게이팅)

**Files:**
- Modify: `lib/airtable.ts`

- [ ] **Step 1: 함수 추가** (Task 4 함수 아래)

공개(isPublic=true) 시 서버에서 필수 항목 재검증. 이미지·premiumId는 payload에 없으므로 현재 레코드에서 읽어 확인.

```ts
/**
 * 크리에이터 포트폴리오/조건/공개 저장.
 * isPublic=true로 전환 시 서버에서 필수 완성 항목 재검증(클라이언트 우회 차단).
 * 미완이면 Error('PROFILE_INCOMPLETE:<빠진항목,>') throw.
 */
export async function updateCreatorProfile(
    creatorId: string,
    payload: CreatorProfileUpdate
): Promise<void> {
    try {
        if (payload.isPublic) {
            const record = await creatorTable().find(creatorId);
            const attachments = (record.get('프로필 이미지') as AirtableAttachment[] | undefined) || [];
            const premiumLinks = record.get('프리미엄 협찬 신청 인플루언서') as string[] | undefined;
            const hasSettlement = Array.isArray(premiumLinks) && premiumLinks.length > 0;

            const missing: string[] = [];
            if (attachments.length === 0) missing.push('프로필 이미지');
            if (!payload.representativeLink.trim()) missing.push('대표 콘텐츠 링크');
            if (payload.visitRegions.length === 0) missing.push('방문 가능 지역');
            if (payload.visitDays.length === 0) missing.push('방문 가능 요일');
            if (payload.acceptSiteTypes.length === 0) missing.push('수용 사이트 종류');
            if (!(payload.minSponsorAmount > 0)) missing.push('최소 협찬 단가');
            if (!hasSettlement) missing.push('정산 정보');

            if (missing.length > 0) {
                throw new Error(`PROFILE_INCOMPLETE:${missing.join(',')}`);
            }
        }

        const fields: Record<string, unknown> = {
            '대표 콘텐츠 링크': payload.representativeLink.trim(),
            '협찬 희망 금액': payload.minSponsorAmount,
            '방문 가능 지역': payload.visitRegions,
            '방문 가능 요일': payload.visitDays,
            '수용 사이트 종류': payload.acceptSiteTypes,
            '프로필 공개': payload.isPublic
        };

        await creatorTable().update([
            { id: creatorId, fields: fields as Partial<FieldSet> }
        ]);
    } catch (error) {
        console.error('Update creator profile error:', error);
        throw error;
    }
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): updateCreatorProfile + 서버 공개 게이팅"
```

---

## Task 6: lib — uploadCreatorProfileImage (Airtable uploadAttachment)

**Files:**
- Modify: `lib/airtable.ts`

- [ ] **Step 1: 함수 추가** (Task 5 함수 아래)

airtable SDK엔 첨부 업로드가 없다 → `content.airtable.com` REST 직접 호출. base64 순수 문자열(data URL 접두어 제거된) 전달.

```ts
/**
 * 크리에이터 프로필 이미지 업로드 → Airtable `프로필 이미지` 첨부 필드에 직접 저장.
 * content.airtable.com uploadAttachment 엔드포인트 사용(외부 호스트 불필요, 파일당 ≤5MB).
 * @param fileBase64  data URL 접두어 제거된 순수 base64 문자열
 * @returns 업로드 후 첫 첨부의 (만료성) URL
 */
export async function uploadCreatorProfileImage(
    creatorId: string,
    fileBase64: string,
    contentType: string,
    filename: string
): Promise<string> {
    const baseId = process.env.AIRTABLE_BASE_ID;
    const token = process.env.AIRTABLE_ACCESS_TOKEN;
    if (!baseId || !token) {
        throw new Error('AIRTABLE_BASE_ID/ACCESS_TOKEN 환경변수 미설정');
    }

    // 첨부 필드명은 URL 인코딩(한글·공백)
    const fieldName = encodeURIComponent('프로필 이미지');
    const url = `https://content.airtable.com/v0/${baseId}/${creatorId}/${fieldName}/uploadAttachment`;

    const response = await fetch(url, {
        method: 'POST',
        headers: {
            Authorization: `Bearer ${token}`,
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({ contentType, filename, file: fileBase64 })
    });

    if (!response.ok) {
        const detail = await response.text();
        console.error('uploadAttachment failed:', response.status, detail);
        throw new Error('IMAGE_UPLOAD_FAILED');
    }

    const data = await response.json();
    // 응답: { fields: { '프로필 이미지': [{ url, ... }] } } 형태
    const attachments = (data?.fields?.['프로필 이미지'] as AirtableAttachment[] | undefined) || [];
    return attachments.length > 0 ? attachments[attachments.length - 1].url : '';
}
```

> ⚠️ 구현 시 Airtable `uploadAttachment` 최신 문서로 엔드포인트 경로·응답 스키마 확인(응답의 `fields` 키 위치가 바뀌면 파싱 조정). 필드명 대신 fieldId(`fldpuIQagshFh2v2s`) 사용도 가능.

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add lib/airtable.ts
git commit -m "feat(jimyeong): uploadCreatorProfileImage — Airtable 첨부 직접 업로드"
```

---

## Task 7: API — GET/PATCH /api/creator/profile

**Files:**
- Create: `app/api/creator/profile/route.ts`

- [ ] **Step 1: 라우트 작성**

```ts
// route.ts - 지명형 크리에이터 프로필 조회/저장 API
import { NextRequest, NextResponse } from 'next/server';
import { cookies } from 'next/headers';
import { jwtVerify } from 'jose';
import { getCreatorProfile, getPremiumSettlement, updateCreatorProfile } from '@/lib/airtable';
import type { CreatorProfileUpdate } from '@/types';

if (!process.env.NEXTAUTH_SECRET) {
    throw new Error('NEXTAUTH_SECRET 환경변수가 설정되지 않았습니다.');
}
const JWT_SECRET = new TextEncoder().encode(process.env.NEXTAUTH_SECRET);

async function getCreatorId(): Promise<string | null> {
    const cookieStore = await cookies();
    const token = cookieStore.get('auth-token')?.value;
    if (!token) return null;
    const { payload } = await jwtVerify(token, JWT_SECRET);
    return ((payload.creatorId || payload.id) as string) || null;
}

export async function GET() {
    try {
        const creatorId = await getCreatorId();
        if (!creatorId) {
            return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
        }

        const [profile, settlement] = await Promise.all([
            getCreatorProfile(creatorId),
            getPremiumSettlement(creatorId)
        ]);

        if (!profile) {
            return NextResponse.json({ error: '프로필을 불러오지 못했습니다.' }, { status: 404 });
        }

        return NextResponse.json({ profile, settlement });
    } catch (error) {
        console.error('Get creator profile route error:', error);
        return NextResponse.json(
            { error: '프로필을 불러오는 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요. 계속되면 카카오톡 채널로 문의해주세요.' },
            { status: 500 }
        );
    }
}

export async function PATCH(request: NextRequest) {
    try {
        const creatorId = await getCreatorId();
        if (!creatorId) {
            return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
        }

        const body = await request.json();
        const payload: CreatorProfileUpdate = {
            representativeLink: typeof body.representativeLink === 'string' ? body.representativeLink : '',
            minSponsorAmount: Number(body.minSponsorAmount) || 0,
            visitRegions: Array.isArray(body.visitRegions) ? body.visitRegions : [],
            visitDays: Array.isArray(body.visitDays) ? body.visitDays : [],
            acceptSiteTypes: Array.isArray(body.acceptSiteTypes) ? body.acceptSiteTypes : [],
            isPublic: body.isPublic === true
        };

        await updateCreatorProfile(creatorId, payload);
        return NextResponse.json({ success: true });
    } catch (error) {
        const message = error instanceof Error ? error.message : '';
        if (message.startsWith('PROFILE_INCOMPLETE:')) {
            const missing = message.slice('PROFILE_INCOMPLETE:'.length).split(',').filter(Boolean);
            return NextResponse.json(
                { error: '공개하려면 필수 항목을 모두 채워주세요.', missing },
                { status: 400 }
            );
        }
        console.error('Patch creator profile route error:', error);
        return NextResponse.json(
            { error: '저장 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요. 계속되면 카카오톡 채널로 문의해주세요.' },
            { status: 500 }
        );
    }
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add app/api/creator/profile/route.ts
git commit -m "feat(jimyeong): GET/PATCH /api/creator/profile"
```

---

## Task 8: API — POST /api/creator/profile/image

**Files:**
- Create: `app/api/creator/profile/image/route.ts`

- [ ] **Step 1: 라우트 작성**

클라이언트가 리사이즈한 data URL을 JSON으로 보낸다. 서버는 접두어 제거 후 Airtable로 전달.

```ts
// route.ts - 지명형 프로필 이미지 업로드 API
import { NextRequest, NextResponse } from 'next/server';
import { cookies } from 'next/headers';
import { jwtVerify } from 'jose';
import { uploadCreatorProfileImage } from '@/lib/airtable';

if (!process.env.NEXTAUTH_SECRET) {
    throw new Error('NEXTAUTH_SECRET 환경변수가 설정되지 않았습니다.');
}
const JWT_SECRET = new TextEncoder().encode(process.env.NEXTAUTH_SECRET);

// data URL 최대 크기(base64 팽창 고려 ~6.7MB → 원본 ≤5MB)
const MAX_DATA_URL_LENGTH = 7_000_000;

export async function POST(request: NextRequest) {
    try {
        const cookieStore = await cookies();
        const token = cookieStore.get('auth-token')?.value;
        if (!token) {
            return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
        }
        const { payload } = await jwtVerify(token, JWT_SECRET);
        const creatorId = ((payload.creatorId || payload.id) as string) || null;
        if (!creatorId) {
            return NextResponse.json({ error: 'creatorId를 찾을 수 없습니다.' }, { status: 400 });
        }

        const body = await request.json();
        const dataUrl: string = body.dataUrl || '';
        const filename: string = body.filename || 'profile.jpg';

        // data:<contentType>;base64,<data>
        const match = /^data:([^;]+);base64,(.+)$/.exec(dataUrl);
        if (!match) {
            return NextResponse.json({ error: '이미지 형식이 올바르지 않습니다.' }, { status: 400 });
        }
        if (dataUrl.length > MAX_DATA_URL_LENGTH) {
            return NextResponse.json({ error: '이미지 용량이 너무 큽니다. 5MB 이하로 올려주세요.' }, { status: 400 });
        }
        const contentType = match[1];
        const fileBase64 = match[2];

        const imageUrl = await uploadCreatorProfileImage(creatorId, fileBase64, contentType, filename);
        return NextResponse.json({ success: true, imageUrl });
    } catch (error) {
        console.error('Upload profile image route error:', error);
        return NextResponse.json(
            { error: '이미지 업로드 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요.' },
            { status: 500 }
        );
    }
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add app/api/creator/profile/image/route.ts
git commit -m "feat(jimyeong): POST /api/creator/profile/image"
```

---

## Task 9: 백엔드 통합 검증 체크포인트

**Files:** 없음 (수동 검증)

- [ ] **Step 1: dev 서버 기동 + 유효 로그인**

preview_start `{name}` 으로 dev 서버 띄우고(`.claude/launch.json` 없으면 `next dev`/포트 3000으로 생성), `/login`에서 실제 테스트 크리에이터로 로그인해 `auth-token` 쿠키 확보.

- [ ] **Step 2: GET 확인**

브라우저에서 `/api/creator/profile` 접속(또는 fetch). 기대: `{ profile: {...}, settlement: {...} }` 200. `profile.channelName`이 로그인 크리에이터와 일치, `settlement.registered`가 premiumId 유무와 맞음.

- [ ] **Step 3: PATCH 게이팅 확인**

빈 상태에서 `isPublic:true`로 PATCH → 400 + `missing` 배열 반환 확인. 조건 채우고 `isPublic:false`로 PATCH → 200. Airtable MCP `list_records`(크리에이터 명단, 해당 record)로 `방문 가능 지역` 등 반영 확인.

- [ ] **Step 4: 이미지 업로드 확인**

작은 이미지 data URL로 POST `/api/creator/profile/image` → 200 + `imageUrl`. Airtable MCP로 `프로필 이미지` 첨부 반영 확인. *(실패 시 Task 6 응답 파싱/엔드포인트 재점검)*

*(체크포인트 — 커밋 없음. 문제 있으면 해당 Task로 돌아가 수정.)*

---

## Task 10: 컴포넌트 — MultiSelectChips (공용)

**Files:**
- Create: `components/MultiSelectChips.tsx`

- [ ] **Step 1: 작성**

```tsx
// MultiSelectChips.tsx - 다중선택 칩(지역/요일/사이트유형 공용)
'use client';

interface MultiSelectChipsProps {
    options: string[];
    selected: string[];
    onChange: (next: string[]) => void;
}

export default function MultiSelectChips({ options, selected, onChange }: MultiSelectChipsProps) {
    function toggle(option: string) {
        if (selected.includes(option)) {
            onChange(selected.filter((value) => value !== option));
        } else {
            onChange([...selected, option]);
        }
    }

    return (
        <div className="flex flex-wrap gap-2">
            {options.map((option) => {
                const active = selected.includes(option);
                return (
                    <button
                        key={option}
                        type="button"
                        onClick={() => toggle(option)}
                        className={`px-3 py-1.5 rounded-full text-sm border transition-colors ${
                            active
                                ? 'bg-brand-bg text-brand-strong border-brand/30 font-medium'
                                : 'bg-card text-ink3 border-line hover:border-brand'
                        }`}
                    >
                        {option}
                    </button>
                );
            })}
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/MultiSelectChips.tsx
git commit -m "feat(jimyeong): MultiSelectChips 공용 컴포넌트"
```

---

## Task 11: 컴포넌트 — AcceptanceConditionFields

**Files:**
- Create: `components/AcceptanceConditionFields.tsx`

- [ ] **Step 1: 작성**

```tsx
// AcceptanceConditionFields.tsx - 협찬 수락 조건 입력(지역/요일/사이트유형/최소단가)
'use client';

import MultiSelectChips from './MultiSelectChips';
import { VISIT_REGIONS, VISIT_DAYS, SPONSOR_SITE_TYPES } from '@/lib/constants';

interface AcceptanceConditionFieldsProps {
    visitRegions: string[];
    visitDays: string[];
    acceptSiteTypes: string[];
    minSponsorAmount: number;
    onChange: (patch: Partial<{
        visitRegions: string[];
        visitDays: string[];
        acceptSiteTypes: string[];
        minSponsorAmount: number;
    }>) => void;
}

export default function AcceptanceConditionFields({
    visitRegions, visitDays, acceptSiteTypes, minSponsorAmount, onChange
}: AcceptanceConditionFieldsProps) {
    return (
        <div className="space-y-5">
            <div>
                <label className="block text-sm font-medium text-ink mb-2">방문 가능 지역</label>
                <MultiSelectChips options={VISIT_REGIONS} selected={visitRegions}
                    onChange={(next) => onChange({ visitRegions: next })} />
            </div>
            <div>
                <label className="block text-sm font-medium text-ink mb-2">방문 가능 요일</label>
                <MultiSelectChips options={VISIT_DAYS} selected={visitDays}
                    onChange={(next) => onChange({ visitDays: next })} />
            </div>
            <div>
                <label className="block text-sm font-medium text-ink mb-2">수용 사이트 종류</label>
                <MultiSelectChips options={SPONSOR_SITE_TYPES} selected={acceptSiteTypes}
                    onChange={(next) => onChange({ acceptSiteTypes: next })} />
            </div>
            <div>
                <label className="block text-sm font-medium text-ink mb-2">
                    최소 협찬 단가 <span className="text-ink3 font-normal">(이 금액 이상 제안만 받아요)</span>
                </label>
                <div className="relative">
                    <input
                        type="number"
                        inputMode="numeric"
                        value={minSponsorAmount || ''}
                        onChange={(event) => onChange({ minSponsorAmount: Number(event.target.value) || 0 })}
                        placeholder="예: 300000"
                        className="w-full h-12 px-4 pr-10 bg-card border border-line rounded-lg text-ink text-sm focus:border-brand focus:outline-none transition-colors placeholder:text-ink3"
                    />
                    <span className="absolute right-4 top-1/2 -translate-y-1/2 text-ink3 text-sm">원</span>
                </div>
            </div>
            <p className="text-xs text-ink3 leading-relaxed">
                조건에 맞는 제안만 도착하고, 캠지기가 선입금하면 개별 승인 없이 자동 확정됩니다.
            </p>
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/AcceptanceConditionFields.tsx
git commit -m "feat(jimyeong): AcceptanceConditionFields 조건 입력"
```

---

## Task 12: 컴포넌트 — SettlementConfirmCard

**Files:**
- Create: `components/SettlementConfirmCard.tsx`

- [ ] **Step 1: 작성**

```tsx
// SettlementConfirmCard.tsx - 정산정보 마스킹 확인 또는 등록 유도
'use client';

import { useRouter } from 'next/navigation';
import type { SettlementSummary } from '@/types';

interface SettlementConfirmCardProps {
    settlement: SettlementSummary;
}

export default function SettlementConfirmCard({ settlement }: SettlementConfirmCardProps) {
    const router = useRouter();

    if (!settlement.registered) {
        return (
            <div className="bg-card border border-line rounded-xl p-5 flex flex-col gap-3">
                <p className="text-sm text-ink2 leading-relaxed">
                    협찬비를 받으려면 정산 정보가 필요해요. 프리미엄 협찬 등록으로 계좌를 먼저 등록해주세요.
                </p>
                <button
                    type="button"
                    onClick={() => router.push('/premium-register')}
                    className="w-full h-12 bg-brand text-black font-bold rounded-lg hover:bg-brand-hover transition-colors"
                >
                    프리미엄 협찬 등록하기
                </button>
            </div>
        );
    }

    return (
        <div className="bg-card border border-line rounded-xl p-5 flex flex-col gap-2">
            <div className="flex items-center gap-2">
                <span className="w-6 h-6 bg-brand-bg rounded-full flex items-center justify-center text-brand-strong text-xs">✓</span>
                <span className="text-sm font-medium text-ink">정산 정보 등록됨</span>
            </div>
            <p className="text-sm text-ink2">
                {settlement.bank} · {settlement.accountHolder} · ****{settlement.accountLast4}
            </p>
            <p className="text-xs text-ink3">수정하려면 카카오톡 채널로 문의해주세요.</p>
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/SettlementConfirmCard.tsx
git commit -m "feat(jimyeong): SettlementConfirmCard 정산 확인/유도"
```

---

## Task 13: 컴포넌트 — ProfileImageUploader (리사이즈+업로드)

**Files:**
- Create: `components/ProfileImageUploader.tsx`

- [ ] **Step 1: 작성**

Canvas로 최대 1024px 리사이즈 → JPEG data URL(품질 0.85) → POST. 외부 라이브러리 없음.

```tsx
// ProfileImageUploader.tsx - 프로필 이미지 선택·리사이즈·업로드·프리뷰
'use client';

import { useRef, useState } from 'react';

interface ProfileImageUploaderProps {
    imageUrl: string;                 // 현재 이미지 URL('' 가능)
    onUploaded: (url: string) => void; // 업로드 성공 시 새 URL 콜백
}

const MAX_DIMENSION = 1024;

async function resizeToDataUrl(file: File): Promise<string> {
    const objectUrl = URL.createObjectURL(file);
    try {
        const img = await new Promise<HTMLImageElement>((resolve, reject) => {
            const image = new Image();
            image.onload = () => resolve(image);
            image.onerror = reject;
            image.src = objectUrl;
        });

        let { width, height } = img;
        if (width > MAX_DIMENSION || height > MAX_DIMENSION) {
            const scale = MAX_DIMENSION / Math.max(width, height);
            width = Math.round(width * scale);
            height = Math.round(height * scale);
        }

        const canvas = document.createElement('canvas');
        canvas.width = width;
        canvas.height = height;
        const ctx = canvas.getContext('2d');
        if (!ctx) throw new Error('canvas context 생성 실패');
        ctx.drawImage(img, 0, 0, width, height);
        return canvas.toDataURL('image/jpeg', 0.85);
    } finally {
        URL.revokeObjectURL(objectUrl);
    }
}

export default function ProfileImageUploader({ imageUrl, onUploaded }: ProfileImageUploaderProps) {
    const inputRef = useRef<HTMLInputElement>(null);
    const [uploading, setUploading] = useState(false);
    const [error, setError] = useState('');

    async function handleFile(event: React.ChangeEvent<HTMLInputElement>) {
        const file = event.target.files?.[0];
        if (!file) return;
        setError('');
        setUploading(true);
        try {
            const dataUrl = await resizeToDataUrl(file);
            const response = await fetch('/api/creator/profile/image', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ dataUrl, filename: file.name })
            });
            const data = await response.json();
            if (!response.ok) {
                setError(data.error || '업로드에 실패했습니다.');
                return;
            }
            onUploaded(data.imageUrl);
        } catch {
            setError('이미지 처리 중 오류가 발생했습니다.');
        } finally {
            setUploading(false);
            if (inputRef.current) inputRef.current.value = '';
        }
    }

    return (
        <div className="flex flex-col items-center gap-3">
            <div className="w-28 h-28 rounded-2xl bg-subtle border border-line overflow-hidden flex items-center justify-center">
                {imageUrl ? (
                    // eslint-disable-next-line @next/next/no-img-element
                    <img src={imageUrl} alt="프로필 이미지" className="w-full h-full object-cover" />
                ) : (
                    <span className="text-ink3 text-xs">이미지 없음</span>
                )}
            </div>
            <button
                type="button"
                onClick={() => inputRef.current?.click()}
                disabled={uploading}
                className="px-4 py-2 bg-subtle text-ink text-sm rounded-lg hover:border-brand border border-line transition-colors disabled:opacity-60"
            >
                {uploading ? '업로드 중...' : imageUrl ? '이미지 변경' : '이미지 업로드'}
            </button>
            <input ref={inputRef} type="file" accept="image/*" onChange={handleFile} className="hidden" />
            {error && <p className="text-red-500 text-xs text-center">{error}</p>}
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/ProfileImageUploader.tsx
git commit -m "feat(jimyeong): ProfileImageUploader 리사이즈+업로드"
```

---

## Task 14: 컴포넌트 — PublishToggle (게이팅 + 동의 모달)

**Files:**
- Create: `components/PublishToggle.tsx`

- [ ] **Step 1: 작성**

부모가 계산한 `missing`(빠진 필수 항목 배열)을 받아, 있으면 안내+차단, 없으면 동의 모달 후 공개.

```tsx
// PublishToggle.tsx - 프로필 공개 토글 + 필수 게이팅 + 자동수락 동의 모달
'use client';

import { useState } from 'react';

interface PublishToggleProps {
    isPublic: boolean;
    missing: string[];                 // 빈 배열이면 공개 가능
    onChange: (next: boolean) => void; // 최종 공개/비공개 확정
}

export default function PublishToggle({ isPublic, missing, onChange }: PublishToggleProps) {
    const [showConsent, setShowConsent] = useState(false);
    const canPublish = missing.length === 0;

    function handleToggleClick() {
        if (isPublic) {
            onChange(false); // 끄기는 자유
            return;
        }
        if (!canPublish) return; // 미완이면 무시(아래 안내 노출)
        setShowConsent(true);
    }

    return (
        <div className="space-y-3">
            <div className="flex items-center justify-between bg-card border border-line rounded-xl p-4">
                <div>
                    <p className="text-sm font-bold text-ink">프로필 공개</p>
                    <p className="text-xs text-ink3 mt-0.5">
                        공개하면 캠지기가 내 프로필을 보고 제안할 수 있어요.
                    </p>
                </div>
                <button
                    type="button"
                    onClick={handleToggleClick}
                    disabled={!isPublic && !canPublish}
                    aria-pressed={isPublic}
                    className={`relative w-12 h-7 rounded-full transition-colors flex-shrink-0 ${
                        isPublic ? 'bg-brand' : 'bg-subtle'
                    } ${!isPublic && !canPublish ? 'opacity-50 cursor-not-allowed' : ''}`}
                >
                    <span className={`absolute top-1 w-5 h-5 rounded-full bg-white transition-transform ${
                        isPublic ? 'translate-x-6' : 'translate-x-1'
                    }`} />
                </button>
            </div>

            {!isPublic && !canPublish && (
                <div className="bg-subtle border border-line rounded-xl p-4">
                    <p className="text-xs text-ink2 mb-2 font-medium">공개하려면 아래를 먼저 완성해주세요</p>
                    <ul className="flex flex-wrap gap-1.5">
                        {missing.map((item) => (
                            <li key={item} className="px-2 py-1 text-xs bg-card border border-line rounded-full text-ink3">
                                {item}
                            </li>
                        ))}
                    </ul>
                </div>
            )}

            {showConsent && (
                <div className="fixed inset-0 z-50 flex items-end justify-center sm:items-center">
                    <div className="absolute inset-0 bg-black/60" onClick={() => setShowConsent(false)} />
                    <div className="relative w-full max-w-md bg-card rounded-t-2xl sm:rounded-2xl p-6 flex flex-col gap-4">
                        <h2 className="text-base font-bold text-ink">공개 전 확인</h2>
                        <p className="text-sm text-ink2 leading-relaxed">
                            공개하면 조건(지역·요일·사이트유형·최소단가)에 맞는 캠지기가 <b className="text-ink">선입금할 때 개별 승인 없이 자동 확정</b>됩니다.
                            원치 않으면 조건을 좁히거나 공개를 끄면 됩니다.
                        </p>
                        <div className="flex gap-2">
                            <button
                                type="button"
                                onClick={() => setShowConsent(false)}
                                className="flex-1 h-12 bg-subtle text-ink rounded-lg hover:border-strong border border-line transition-colors"
                            >
                                취소
                            </button>
                            <button
                                type="button"
                                onClick={() => { setShowConsent(false); onChange(true); }}
                                className="flex-1 h-12 bg-brand text-black font-bold rounded-lg hover:bg-brand-hover transition-colors"
                            >
                                동의하고 공개
                            </button>
                        </div>
                    </div>
                </div>
            )}
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/PublishToggle.tsx
git commit -m "feat(jimyeong): PublishToggle 게이팅+자동수락 동의"
```

---

## Task 15: 컴포넌트 — PortfolioEditForm (합성 + 로드/저장)

**Files:**
- Create: `components/PortfolioEditForm.tsx`

- [ ] **Step 1: 작성**

4섹션 합성. 로드(GET), 저장(PATCH), 클라이언트 게이팅 계산(공개 필수 항목). `SectionTitle`은 PremiumRegisterForm 스타일 재현.

```tsx
// PortfolioEditForm.tsx - 지명형 크리에이터 포트폴리오 편집(4섹션)
'use client';

import { useState, useEffect } from 'react';
import type { CreatorProfile, SettlementSummary, CreatorProfileUpdate } from '@/types';
import ProfileImageUploader from './ProfileImageUploader';
import AcceptanceConditionFields from './AcceptanceConditionFields';
import SettlementConfirmCard from './SettlementConfirmCard';
import PublishToggle from './PublishToggle';

function SectionTitle({ title, desc }: { title: string; desc?: string }) {
    return (
        <div className="pt-2">
            <h3 className="text-base font-bold text-ink">{title}</h3>
            {desc && <p className="text-xs text-ink3 mt-1">{desc}</p>}
            <div className="mt-2 h-px bg-subtle" />
        </div>
    );
}

export default function PortfolioEditForm() {
    const [profile, setProfile] = useState<CreatorProfile | null>(null);
    const [settlement, setSettlement] = useState<SettlementSummary | null>(null);
    const [loading, setLoading] = useState(true);
    const [saving, setSaving] = useState(false);
    const [message, setMessage] = useState('');
    const [error, setError] = useState('');

    useEffect(() => {
        (async () => {
            try {
                const response = await fetch('/api/creator/profile');
                const data = await response.json();
                if (response.ok) {
                    setProfile(data.profile);
                    setSettlement(data.settlement);
                } else {
                    setError(data.error || '프로필을 불러오지 못했습니다.');
                }
            } catch {
                setError('네트워크 오류가 발생했습니다. 새로고침해주세요.');
            } finally {
                setLoading(false);
            }
        })();
    }, []);

    function patchProfile(patch: Partial<CreatorProfile>) {
        setProfile((prev) => (prev ? { ...prev, ...patch } : prev));
        setMessage('');
    }

    // 공개 필수 게이팅 (클라이언트 표시용 — 서버가 최종 검증)
    function computeMissing(p: CreatorProfile, s: SettlementSummary | null): string[] {
        const missing: string[] = [];
        if (!p.hasProfileImage) missing.push('프로필 이미지');
        if (!p.representativeLink.trim()) missing.push('대표 콘텐츠 링크');
        if (p.visitRegions.length === 0) missing.push('방문 가능 지역');
        if (p.visitDays.length === 0) missing.push('방문 가능 요일');
        if (p.acceptSiteTypes.length === 0) missing.push('수용 사이트 종류');
        if (!(p.minSponsorAmount > 0)) missing.push('최소 협찬 단가');
        if (!s?.registered) missing.push('정산 정보');
        return missing;
    }

    async function save(nextIsPublic: boolean) {
        if (!profile) return;
        setSaving(true);
        setError('');
        setMessage('');
        const payload: CreatorProfileUpdate = {
            representativeLink: profile.representativeLink,
            minSponsorAmount: profile.minSponsorAmount,
            visitRegions: profile.visitRegions,
            visitDays: profile.visitDays,
            acceptSiteTypes: profile.acceptSiteTypes,
            isPublic: nextIsPublic
        };
        try {
            const response = await fetch('/api/creator/profile', {
                method: 'PATCH',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(payload)
            });
            const data = await response.json();
            if (!response.ok) {
                setError(data.error || '저장에 실패했습니다.');
                return;
            }
            patchProfile({ isPublic: nextIsPublic });
            setMessage(nextIsPublic ? '공개되었습니다.' : '저장되었습니다.');
        } catch {
            setError('네트워크 오류가 발생했습니다.');
        } finally {
            setSaving(false);
        }
    }

    if (loading) {
        return (
            <div className="flex justify-center py-20">
                <div className="w-10 h-10 border-4 border-brand border-t-transparent rounded-full animate-spin" />
            </div>
        );
    }
    if (!profile) {
        return <p className="text-center text-ink3 py-20">{error || '프로필을 불러오지 못했습니다.'}</p>;
    }

    const missing = computeMissing(profile, settlement);

    return (
        <div className="space-y-6">
            {error && (
                <div className="bg-red-500/10 border border-red-500/30 rounded-xl p-4">
                    <p className="text-red-500 text-sm text-center font-medium">{error}</p>
                </div>
            )}
            {message && (
                <div className="bg-brand-bg border border-brand/30 rounded-xl p-3">
                    <p className="text-brand-strong text-sm text-center font-medium">{message}</p>
                </div>
            )}

            {/* 섹션 1 — 포트폴리오 */}
            <SectionTitle title="포트폴리오" desc="캠지기가 보는 내 소개" />
            <ProfileImageUploader
                imageUrl={profile.profileImageUrl}
                onUploaded={(url) => patchProfile({ profileImageUrl: url, hasProfileImage: !!url })}
            />
            <div className="text-center">
                <p className="text-sm font-bold text-ink">{profile.channelName}</p>
                <p className="text-xs text-ink3 mt-0.5">
                    {'⭐'.repeat(Number(profile.tier) || 0)} · {profile.followerRange || '구독자 미기재'}
                </p>
            </div>
            <div>
                <label className="block text-sm font-medium text-ink mb-2">대표 콘텐츠 링크</label>
                <input
                    type="url"
                    value={profile.representativeLink}
                    onChange={(event) => patchProfile({ representativeLink: event.target.value })}
                    placeholder="https://..."
                    className="w-full h-12 px-4 bg-card border border-line rounded-lg text-ink text-sm focus:border-brand focus:outline-none transition-colors placeholder:text-ink3"
                />
            </div>

            {/* 섹션 2 — 협찬 수락 조건 */}
            <SectionTitle title="협찬 수락 조건" desc="이 조건에 맞는 제안만 받아요" />
            <AcceptanceConditionFields
                visitRegions={profile.visitRegions}
                visitDays={profile.visitDays}
                acceptSiteTypes={profile.acceptSiteTypes}
                minSponsorAmount={profile.minSponsorAmount}
                onChange={patchProfile}
            />

            {/* 섹션 3 — 정산 정보 */}
            <SectionTitle title="정산 정보" desc="협찬비를 받을 계좌" />
            {settlement && <SettlementConfirmCard settlement={settlement} />}

            {/* 저장 (비공개 저장) */}
            <button
                type="button"
                onClick={() => save(profile.isPublic)}
                disabled={saving}
                className="w-full h-12 bg-subtle text-ink font-bold rounded-lg hover:border-strong border border-line transition-colors disabled:opacity-60"
            >
                {saving ? '저장 중...' : '저장'}
            </button>

            {/* 섹션 4 — 공개 */}
            <SectionTitle title="공개" />
            <PublishToggle
                isPublic={profile.isPublic}
                missing={missing}
                onChange={(next) => save(next)}
            />
        </div>
    );
}
```

- [ ] **Step 2: lint** — `npm run lint` → 통과

- [ ] **Step 3: commit**

```bash
git add components/PortfolioEditForm.tsx
git commit -m "feat(jimyeong): PortfolioEditForm 4섹션 합성+로드/저장"
```

---

## Task 16: 페이지 + 대시보드 진입 배너

**Files:**
- Create: `app/dashboard/portfolio/page.tsx`
- Modify: `app/dashboard/page.tsx` (진입 배너 추가)

- [ ] **Step 1: 페이지 작성**

`app/dashboard/portfolio/page.tsx`:

```tsx
// page.tsx - 지명형 크리에이터 포트폴리오 편집 페이지
'use client';

import { useRouter } from 'next/navigation';
import PortfolioEditForm from '@/components/PortfolioEditForm';

export default function PortfolioPage() {
    const router = useRouter();
    return (
        <div className="min-h-screen bg-page pb-20">
            <header className="sticky top-0 z-10 bg-page/95 backdrop-blur-sm border-b border-line">
                <div className="max-w-md mx-auto px-5 py-4 flex items-center gap-3">
                    <button
                        onClick={() => router.push('/dashboard')}
                        className="p-2 -ml-2 text-ink3 hover:text-ink transition-colors"
                        aria-label="대시보드로 돌아가기"
                    >
                        <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
                        </svg>
                    </button>
                    <h1 className="text-lg font-bold text-ink">내 협찬 프로필</h1>
                </div>
            </header>
            <main className="max-w-md mx-auto px-5 py-6">
                <PortfolioEditForm />
            </main>
        </div>
    );
}
```

- [ ] **Step 2: 대시보드 진입 배너 추가**

`app/dashboard/page.tsx`의 메인 영역, 기존 `ContentEntryBanner`가 렌더되는 블록 아래(라인 288 부근 `</div>` 뒤)에 배너 추가:

```tsx
                {!showContentView && userInfo && (
                    <div className="mb-4">
                        <button
                            onClick={() => router.push('/dashboard/portfolio')}
                            className="w-full flex items-center justify-between bg-card border border-line rounded-xl p-4 hover:border-brand transition-colors text-left"
                        >
                            <div>
                                <p className="text-sm font-bold text-ink">내 협찬 프로필</p>
                                <p className="text-xs text-ink3 mt-0.5">조건을 걸고 공개하면 캠지기가 제안해요</p>
                            </div>
                            <svg className="w-5 h-5 text-ink3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
                            </svg>
                        </button>
                    </div>
                )}
```

> 위치: 기존 `{!showContentView && userInfo && (<div className="mb-4"><ContentEntryBanner .../></div>)}` 블록 **바로 아래**에 삽입. `router`는 이미 선언되어 있음.

- [ ] **Step 3: lint + build** — `npm run lint && npm run build` → 통과

- [ ] **Step 4: commit**

```bash
git add app/dashboard/portfolio/page.tsx app/dashboard/page.tsx
git commit -m "feat(jimyeong): 포트폴리오 페이지 + 대시보드 진입 배너"
```

---

## Task 17: 종단 검증 + 회귀 + 빌드

**Files:** 없음 (수동 검증)

- [ ] **Step 1: dev 프리뷰 종단 시나리오**

preview로 로그인 → 대시보드 "내 협찬 프로필" 배너 클릭 → `/dashboard/portfolio` 진입 →
1. 이미지 업로드 → 프리뷰 반영 확인
2. 대표 링크·지역·요일·사이트유형·최소단가 입력 → [저장] → "저장되었습니다"
3. 공개 토글: 하나 비우면 토글 비활성 + 빠진 항목 칩 노출 확인
4. 전부 채우고 토글 → 동의 모달 → [동의하고 공개] → "공개되었습니다"
5. Airtable MCP `list_records`로 크리에이터 명단 해당 record의 `방문 가능 지역`·`수용 사이트 종류`·`프로필 공개`·`프로필 이미지` 반영 확인

- [ ] **Step 2: 정산 분기 확인**

premiumId 없는 테스트 크리에이터로 로그인 → 정산 섹션이 "프리미엄 협찬 등록하기" 버튼 노출, 공개 게이팅에 "정산 정보" 포함 확인.

- [ ] **Step 3: 회귀 확인**

기존 `/login` 로그인, `/dashboard` 캠페인/콘텐츠 뷰, `/premium-register`, 알림 토글 정상 동작 확인.

- [ ] **Step 4: 빌드**

Run: `npm run lint && npm run build`
Expected: 둘 다 성공.

- [ ] **Step 5: 최종 커밋(없으면 skip) + 브랜치 푸시(사용자 승인 시)**

```bash
git status   # 미커밋 변경 없어야 함
# 푸시는 사용자 승인 후: git push -u origin feat/jimyeong-portfolio-1a
```

---

## Self-Review (스펙 대조)

- **§2 IN ① 편집 페이지** → Task 15·16 ✅
- **§2 IN ② 조회/저장 API** → Task 7 ✅
- **§2 IN ③ 이미지 업로드** → Task 6·8·13 ✅
- **§2 IN ④ 공개 게이팅 + 동의** → Task 5(서버)·14(클라)·15(계산) ✅
- **§2 IN ⑤ 진입 배너** → Task 16 ✅
- **§4 4섹션** → Task 15 ✅ / **§5 필수 항목** → Task 5·15 동일 목록 ✅
- **§6 필드 매핑** → Task 3·5 실제 필드명 사용 ✅ / **§8 uploadAttachment** → Task 6 ✅
- **§9 보안(IDOR·마스킹·JWT)** → Task 4·5·7·8 ✅ / **§12 선행(필드 신설)** → P1 ✅
- 타입 일관성: `CreatorProfile`/`SettlementSummary`/`CreatorProfileUpdate` Task 2 정의 → 3·5·7·15에서 동일 사용 ✅
- 미커버: 없음. (1b·포털 밖은 의도적 제외)
